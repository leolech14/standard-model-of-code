# Session Handoff: March 17, 2026

> Load this AFTER `atlas/ATLAS.yaml`. This tells you what just happened and what to do next.

---

## Who Was Here

Two Claude Code sessions ran in parallel on March 15-17:

**Session A (this handoff's author, "Atlas Architect"):**
Built the Ecosystem Atlas framework, Collider emitter (Stage 23), Oracle Tier -1, channels architecture, _inbox audit, governance coherence audit. Biography at `.ecoroot/session-history/session-biography-atlas-architect-2026-03-15-17.md`.

**Session B (DevJournal + Ideome):**
Built DevJournal ingestion pipeline (3 collectors, compiler, materializer), ideome_scorer.py (v0, broken — see critique), atlas enrichment sweep (F→D), SMoC consolidation Phase 0+1 (WHAT_IS_SMOC.md, monolith deprecation), ETS signature protocol.

Both sessions are now closed. No agents are actively editing files.

---

## Ecosystem State Right Now

```
atlas/ATLAS.yaml          65+ entities (33 CMP, 4 AGT, 11 CON, 3 RES, 14 EXT)
atlas/schemas/            6 schema files (5 kinds, 9 relationships)
atlas/SCAFFOLD_HYPOTHESIS_VALIDATION.md   CONFIRMED WITH BOUNDARIES
atlas/DISCOVERABILITY_REPORT.md           Score: 4/5
```

```
.ecoroot/                 5 knowledge directories:
├── color-science/        Color theory research (Pillar 2)
├── exploration-maps/     6 cross-session synthesis docs
├── opportunities/        2 actionable possibilities
├── session-history/      8 session records (institutional memory)
└── (17 loose root files at PROJECTS_all/.ecoroot/ — see categorization below)
```

```
governance/
├── OPEN_CONCERNS.md      42 concerns, 5 critical, 13 high
├── IDEOME_ARCHITECTURE_CRITIQUE.md   v0 scorer is broken (checks YAML not code)
└── ROADMAP.md            Ecosystem roadmap
```

---

## DO NOT TOUCH (other agents' territory or actively unstable)

| File/Area | Why |
|-----------|-----|
| `wave/tools/ai/ideome_scorer.py` | v0 is broken by design. Critique exists. v2 needs full brainstorm, not a patch. |
| `wave/tools/ai/analyze.py` | Session B edited it (P3 promotion). Stable now but complex (3678 lines). |
| `governance/OPEN_CONCERNS.md` | Session B populated it. 42 items, well-structured. Read, don't restructure. |
| `wave/tools/devjournal/` | Session B built this. 7 files, working pipeline. Don't modify without reading the handoff. |
| `PROJECTS_all/.ecoroot/*.yaml` | Shared ecosystem config. Multiple projects depend on these. Don't reorganize. |

---

## HIGH-WIN UNCONFLICTED TASKS (safe to tackle right now)

### 1. Wire Oracle Tier -1 into ACI routing (30 min, zero conflict)

**What:** `wave/tools/ai/analyze/tiers/atlas.py` exists and is tested but NOT hooked into the ACI flow. Every `analyze.py --aci` query should check the atlas BEFORE calling any LLM.

**Where:** The insertion point is in the ACI tier routing logic. Add an atlas lookup as Tier -1 that fires before Tier 0 (INSTANT).

**Test:** `python analyze.py --aci "what connects to Cerebras?"` should return CON-001's explanation without an LLM call.

**Files to edit:** `wave/tools/ai/analyze.py` (one insertion point in ACI routing)
**Files to read first:** `wave/tools/ai/analyze/tiers/atlas.py` (the module to wire in)

---

### 2. Register atlas emitter as CMP-092 (15 min, zero conflict)

**What:** Collider Stage 23 (`particle/src/core/pipeline/stages/atlas_emitter.py`) auto-detects components but has NO atlas entry itself. 250 lines of working code, invisible to the scaffold.

**Where:** Add to `atlas/ATLAS.yaml` under `components:` section. Use the `new-ecosystem-component` skill at `~/.claude/skills/new-ecosystem-component.md`.

**ID:** CMP-092 (check `grep 'id: CMP-' atlas/ATLAS.yaml | sort | tail -5` for latest)

---

### 3. Parent .ecoroot/ organization plan (45 min, read-only)

**What:** `PROJECTS_all/.ecoroot/` has 17 loose files at root level. They fall into 5 categories:
- **Registries** (TOOLS_REGISTRY.yaml, manifest.yaml, ecosync.yaml, IDEOME.yaml) — stay or get `registries/` dir
- **Config** (common_knowledge.md, ecosync_offload.py) — stay at root (critical imports)
- **Tracking** (current_concerns.md, ECOSYNC_DIAGNOSTIC.md, ECOSYSTEM_SCORECARD.md) — should consolidate with `governance/OPEN_CONCERNS.md`
- **Audit output** (AUDIT_PROMPT.md, AUDIT_REPORT.md) — should go to `session-history/`
- **Docs** (README.md, ECOSYSTEM_INGEST.md, ECOSYSTEM_GLOSSARY.md) — review for staleness

**Action:** Write the organization plan as an exploration map. DON'T move files — `common_knowledge.md` is imported by multiple agents.

---

### 4. Perplexity API quota fix (5 min, operational)

**What:** CON-007 (Perplexity Sonar API) is `status: broken` in ATLAS.yaml. Quota exhausted March 15.

**Action:** Check Perplexity billing at perplexity.ai. Recharge credits. Update CON-007 health status to `active` + `last_verified` to today.

---

### 5. _inbox high-confidence archiving (30 min, zero conflict)

**What:** The _inbox audit identified 3 high-confidence archive buckets:
- **Bucket C** (Orientation, 15 files, 297K) — Jan 2026 snapshots, all superseded. 99% confidence.
- **Bucket D** (Chat Threads, 12 files, 7.5MB) — Raw conversation dumps. 95% confidence.
- **Bucket E** (Infra Audits, 33 files, 317K) — Oct-Dec 2025 GCP/Doppler plans. 95% confidence.

**Evidence:** `_inbox/SMOC_EXPLORATION_LAYER1.md` and `_inbox/SMOC_EXPLORATION_LAYER2.md`

**Action:** Create `_inbox/archive-smoc-20260317/` and move these 60 files. Read the evidence docs first.

---

### 6. Theory docs identity blocks (1 hour, zero conflict)

**What:** 277 theory files in `particle/docs/theory/` have zero atlas-style identity. The SMoC consolidation plan's Phase 5 calls for adding identity blocks to framework files.

**Start with:** The 9 framework files in `particle/docs/theory/frameworks/` — they already have YAML frontmatter. Extend each with:
```yaml
kind: theory_document
owner: leo
stage: P2
agent:
  explanation: "One-sentence summary for AI agents"
  context_priority: 0.5
```

**This doesn't need the artifact node type** — it's just adding metadata to existing files. The artifact schema extension is a separate decision.

---

## MEDIUM-WIN TASKS (need more context or design first)

| Task | Effort | Blocker |
|------|--------|---------|
| Artifact node type (6th kind) | 2h | Design seed at `memory/artifact_node_type_design.md`. Needs schema discussion. |
| Ideome scorer v2 | 4h+ | Needs brainstorming. Critique at `governance/IDEOME_ARCHITECTURE_CRITIQUE.md`. Build on `ideome_synthesis.py` not v0. |
| DevJournal Refinery page | 3h | Session B built the pipeline. Page needs `app/api/v1/journal/route.ts` + `app/(pages)/journal/page.tsx`. |
| Channels Phase 2 (app.py wiring) | 2h | Code at `PROJECT_openclaw/dashboard/channels/`. Needs `app.py` route mounting + `llm_proxy.py` tool injection. |

---

## KEY FILES TO READ

| Priority | File | Why |
|----------|------|-----|
| FIRST | `atlas/ATLAS.yaml` | The ecosystem context. CLAUDE.md tells you to load it. |
| SECOND | This file | You're reading it. |
| IF BUILDING | `~/.claude/skills/new-ecosystem-component.md` | Checklist for any new component. Follow it. |
| IF DEBUGGING | `governance/OPEN_CONCERNS.md` | 42 tracked concerns with severity. |
| IF RESEARCHING | `.ecoroot/exploration-maps/` | 6 synthesis docs covering architecture decisions. |
| IF CURIOUS | `.ecoroot/session-history/session-biography-atlas-architect-2026-03-15-17.md` | Full story of how the atlas was built. |

---

## ETS SIGNATURE (required on all commits)

```
Agent-Model: [your-model-id]
Agent-Access: cli
Agent-Orchestration: direct

Co-Authored-By: [Your Model] <noreply@anthropic.com>
```

See global `~/.claude/CLAUDE.md` for the full ETS protocol.
