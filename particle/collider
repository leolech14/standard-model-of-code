#!/usr/bin/env bash
# Collider CLI wrapper
# This script wraps cli.py for easy invocation as ./collider

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/cli.py" "$@"
