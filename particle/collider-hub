#!/usr/bin/env bash
# Collider Hub wrapper
# This script wraps scripts/collider_hub.py for easy invocation as ./collider-hub

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/scripts/collider_hub.py" "$@"
