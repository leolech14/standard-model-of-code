version: 1.0.0
generated_by: mine_semgrep.py
source: semgrep-rules
atom_count: 566
atoms:
- id: EXT.PY.CORR.001
  name: Sock Shutdown
  ecosystem: python
  category: correctness
  description: Socket is not closed if shutdown fails. When socket.shutdown fails
    on an OSError, socket.close is not called and the code fails to clean up the socket
    and allow garbage collection to release the memor
  patterns:
  - $SOCK.shutdown(socket.$A)
  source_rules:
  - socket-shutdown-close
  tier: T2
- id: EXT.PY.CORR.002
  name: Shutdown
  ecosystem: python
  category: correctness
  description: Socket is not closed if shutdown fails. When socket.shutdown fails
    on an OSError, socket.close is not called and the code fails to clean up the socket
    and allow garbage collection to release the memor
  patterns:
  - $SOCK.shutdown(socket.$A)
  source_rules:
  - socket-shutdown-close
  tier: T2
- id: EXT.PY.SEC.003
  name: Format
  ecosystem: python
  category: security
  description: Using non-constant TwiML (Twilio Markup Language) argument when creating
    a Twilio conversation could allow the injection of additional TwiML commands
  patterns:
  - '"...".format(...)'
  - $DB.execute("...".format(...))
  source_rules:
  - formatted-sql-query
  - twiml-injection
  tier: T2
- id: EXT.PY.SEC.004
  name: Xml Sax Saxutils Escape
  ecosystem: python
  category: security
  description: Using non-constant TwiML (Twilio Markup Language) argument when creating
    a Twilio conversation could allow the injection of additional TwiML commands
  patterns:
  - xml.sax.saxutils.escape(...)
  source_rules:
  - twiml-injection
  tier: T2
- id: EXT.PY.SEC.005
  name: Escape
  ecosystem: python
  category: security
  description: Using non-constant TwiML (Twilio Markup Language) argument when creating
    a Twilio conversation could allow the injection of additional TwiML commands
  patterns:
  - html.escape(...)
  - xml.sax.saxutils.escape(...)
  source_rules:
  - twiml-injection
  tier: T2
- id: EXT.PY.SEC.006
  name: Html Escape
  ecosystem: python
  category: security
  description: Using non-constant TwiML (Twilio Markup Language) argument when creating
    a Twilio conversation could allow the injection of additional TwiML commands
  patterns:
  - html.escape(...)
  source_rules:
  - twiml-injection
  tier: T2
- id: EXT.PY.SEC.007
  name: Client Calls Create
  ecosystem: python
  category: security
  description: Using non-constant TwiML (Twilio Markup Language) argument when creating
    a Twilio conversation could allow the injection of additional TwiML commands
  patterns:
  - $CLIENT.calls.create(..., twiml=$SINK, ...)
  source_rules:
  - twiml-injection
  tier: T2
- id: EXT.PY.SEC.008
  name: Create
  ecosystem: python
  category: security
  description: Using non-constant TwiML (Twilio Markup Language) argument when creating
    a Twilio conversation could allow the injection of additional TwiML commands
  patterns:
  - $CLIENT.calls.create(..., twiml=$SINK, ...)
  source_rules:
  - twiml-injection
  tier: T2
- id: EXT.PY.SEC.009
  name: App Add Middleware
  ecosystem: python
  category: security
  description: CORS policy allows any origin (using wildcard '*'). This is insecure
    and should be avoided.
  patterns:
  - $APP.add_middleware(
  source_rules:
  - wildcard-cors
  tier: T2
- id: EXT.PY.SEC.010
  name: Add Middleware
  ecosystem: python
  category: security
  description: CORS policy allows any origin (using wildcard '*'). This is insecure
    and should be avoided.
  patterns:
  - $APP.add_middleware(
  source_rules:
  - wildcard-cors
  tier: T2
- id: EXT.PY.COMPAT.011
  name: Importlib Source Hash
  ecosystem: python
  category: compatibility
  description: source_hash' is only available on Python 3.7+. This does not work in
    lower versions, and therefore is not backwards compatible. Instead, use another
    hash function.
  patterns:
  - importlib.source_hash()
  source_rules:
  - python37-compatibility-importlib
  tier: T2
- id: EXT.PY.COMPAT.012
  name: Source Hash
  ecosystem: python
  category: compatibility
  description: source_hash' is only available on Python 3.7+. This does not work in
    lower versions, and therefore is not backwards compatible. Instead, use another
    hash function.
  patterns:
  - importlib.source_hash()
  source_rules:
  - python37-compatibility-importlib
  tier: T2
- id: EXT.PY.COMPAT.013
  name: Importlib Resources
  ecosystem: python
  category: compatibility
  description: Found 'importlib.resources', which is a module only available on Python
    3.7+. This does not work in lower versions, and therefore is not backwards compatible.
    Use importlib_resources instead for older
  patterns:
  - import importlib.resources
  source_rules:
  - python37-compatibility-importlib2
  tier: T2
- id: EXT.PY.COMPAT.014
  name: Http Client Httpconnection
  ecosystem: python
  category: compatibility
  description: 'Found usage of the ''blocksize'' argument in a HTTPConnection call.
    This is only available on Python 3.7+ and is therefore not backwards compatible.
    Remove this in order for this code to work in Python '
  patterns:
  - http.client.HTTPConnection(blocksize=$X,...)
  source_rules:
  - python37-compatibility-httpconn
  tier: T2
- id: EXT.PY.COMPAT.015
  name: Httpconnection
  ecosystem: python
  category: compatibility
  description: 'Found usage of the ''blocksize'' argument in a HTTPConnection call.
    This is only available on Python 3.7+ and is therefore not backwards compatible.
    Remove this in order for this code to work in Python '
  patterns:
  - http.client.HTTPConnection(blocksize=$X,...)
  source_rules:
  - python37-compatibility-httpconn
  tier: T2
- id: EXT.PY.SEC.016
  name: Http Client Httpsconnection
  ecosystem: python
  category: security
  description: Found usage of the 'blocksize' argument in a HTTPSConnection call.
    This is only available on Python 3.7+ and is therefore not backwards compatible.
    Remove this in order for this code to work in Python
  patterns:
  - http.client.HTTPSConnection(...)
  - http.client.HTTPSConnection(blocksize=$X,...)
  source_rules:
  - python37-compatibility-httpsconn
  - httpsconnection-detected
  tier: T2
- id: EXT.PY.SEC.017
  name: Httpsconnection
  ecosystem: python
  category: security
  description: Found usage of the 'blocksize' argument in a HTTPSConnection call.
    This is only available on Python 3.7+ and is therefore not backwards compatible.
    Remove this in order for this code to work in Python
  patterns:
  - six.moves.http_client.HTTPSConnection(...)
  - httplib.HTTPSConnection(...)
  - http.client.HTTPSConnection(...)
  - http.client.HTTPSConnection(blocksize=$X,...)
  source_rules:
  - python37-compatibility-httpsconn
  - httpsconnection-detected
  tier: T2
- id: EXT.PY.COMPAT.018
  name: Importlib Abc Resourcereader
  ecosystem: python
  category: compatibility
  description: Found usage of 'importlib.abc.ResourceReader'. This module is only
    available on Python 3.7+ and is therefore not backwards compatible. Instead, use
    another loader.
  patterns:
  - import importlib.abc.ResourceReader
  source_rules:
  - python37-compatibility-importlib3
  tier: T2
- id: EXT.PY.COMPAT.019
  name: Textiowrapper Reconfigure
  ecosystem: python
  category: compatibility
  description: Found usage of 'importlib.abc.ResourceReader'. This module is only
    available on Python 3.7+ and is therefore not backwards compatible. Instead, use
    another loader.
  patterns:
  - TextIOWrapper.reconfigure(...)
  source_rules:
  - python37-compatibility-textiowrapper
  tier: T2
- id: EXT.PY.COMPAT.020
  name: Reconfigure
  ecosystem: python
  category: compatibility
  description: Found usage of 'importlib.abc.ResourceReader'. This module is only
    available on Python 3.7+ and is therefore not backwards compatible. Instead, use
    another loader.
  patterns:
  - TextIOWrapper.reconfigure(...)
  source_rules:
  - python37-compatibility-textiowrapper
  tier: T2
- id: EXT.PY.COMPAT.021
  name: Ipaddress Ipv6Network Subnet Of
  ecosystem: python
  category: compatibility
  description: IPv6Network.subnet_of is only available on Python 3.7+ and is therefore
    not backwards compatible. Instead, check if the subnet is in 'subnets'.
  patterns:
  - ipaddress.IPv6Network.subnet_of($X)
  source_rules:
  - python37-compatibility-ipv6network1
  tier: T2
- id: EXT.PY.COMPAT.022
  name: Subnet Of
  ecosystem: python
  category: compatibility
  description: IPv6Network.subnet_of is only available on Python 3.7+ and is therefore
    not backwards compatible. Instead, check if the subnet is in 'subnets'.
  patterns:
  - ipaddress.IPv4Network.subnet_of($X)
  - ipaddress.IPv6Network.subnet_of($X)
  source_rules:
  - python37-compatibility-ipv6network1
  - python37-compatibility-ipv4network1
  tier: T2
- id: EXT.PY.COMPAT.023
  name: Ipaddress Ipv6Network Supernet Of
  ecosystem: python
  category: compatibility
  description: IPv6Network.supernet_of is only available on Python 3.7+ and is therefore
    not backwards compatible. Instead, check if the supernet is in 'supernet'.
  patterns:
  - ipaddress.IPv6Network.supernet_of($X)
  source_rules:
  - python37-compatibility-ipv6network2
  tier: T2
- id: EXT.PY.COMPAT.024
  name: Supernet Of
  ecosystem: python
  category: compatibility
  description: IPv6Network.supernet_of is only available on Python 3.7+ and is therefore
    not backwards compatible. Instead, check if the supernet is in 'supernet'.
  patterns:
  - ipaddress.IPv6Network.supernet_of($X)
  - ipaddress.IPv4Network.supernet_of($X)
  source_rules:
  - python37-compatibility-ipv4network2
  - python37-compatibility-ipv6network2
  tier: T2
- id: EXT.PY.COMPAT.025
  name: Ipaddress Ipv4Network Subnet Of
  ecosystem: python
  category: compatibility
  description: IPv4Network.subnet_of is only available on Python 3.7+ and is therefore
    not backwards compatible. Instead, check if the subnet is in 'subnets'.
  patterns:
  - ipaddress.IPv4Network.subnet_of($X)
  source_rules:
  - python37-compatibility-ipv4network1
  tier: T2
- id: EXT.PY.COMPAT.026
  name: Ipaddress Ipv4Network Supernet Of
  ecosystem: python
  category: compatibility
  description: IPv4Network.supernet_of is only available on Python 3.7+ and is therefore
    not backwards compatible. Instead, check if the supernet is in 'supernet'.
  patterns:
  - ipaddress.IPv4Network.supernet_of($X)
  source_rules:
  - python37-compatibility-ipv4network2
  tier: T2
- id: EXT.PY.COMPAT.027
  name: Locale Format String
  ecosystem: python
  category: compatibility
  description: Found usage of the 'monetary' argument in a function call of 'locale.format_string'.
    This is only available on Python 3.7+ and is therefore not backwards compatible.
    Instead, remove the 'monetary' arg
  patterns:
  - locale.format_string(monetary=$X, ...)
  source_rules:
  - python37-compatibility-locale1
  tier: T2
- id: EXT.PY.COMPAT.028
  name: Format String
  ecosystem: python
  category: compatibility
  description: Found usage of the 'monetary' argument in a function call of 'locale.format_string'.
    This is only available on Python 3.7+ and is therefore not backwards compatible.
    Instead, remove the 'monetary' arg
  patterns:
  - locale.format_string(monetary=$X, ...)
  source_rules:
  - python37-compatibility-locale1
  tier: T2
- id: EXT.PY.COMPAT.029
  name: Math Remainder
  ecosystem: python
  category: compatibility
  description: math.remainder is only available on Python 3.7+ and is therefore not
    backwards compatible. Instead, use math.fmod() or calculate $X - n* $Y.
  patterns:
  - math.remainder($X, $Y)
  source_rules:
  - python37-compatibility-math1
  tier: T2
- id: EXT.PY.COMPAT.030
  name: Remainder
  ecosystem: python
  category: compatibility
  description: math.remainder is only available on Python 3.7+ and is therefore not
    backwards compatible. Instead, use math.fmod() or calculate $X - n* $Y.
  patterns:
  - math.remainder($X, $Y)
  source_rules:
  - python37-compatibility-math1
  tier: T2
- id: EXT.PY.COMPAT.031
  name: Multiprocessing Process Close
  ecosystem: python
  category: compatibility
  description: multiprocessing.Process.close() is only available on Python 3.7+ and
    is therefore not backwards compatible. Instead, use join().
  patterns:
  - multiprocessing.Process.close()
  source_rules:
  - python37-compatibility-multiprocess1
  tier: T2
- id: EXT.PY.COMPAT.032
  name: Close
  ecosystem: python
  category: compatibility
  description: multiprocessing.Process.close() is only available on Python 3.7+ and
    is therefore not backwards compatible. Instead, use join().
  patterns:
  - multiprocessing.Process.close()
  source_rules:
  - python37-compatibility-multiprocess1
  tier: T2
- id: EXT.PY.COMPAT.033
  name: Multiprocessing Process Kill
  ecosystem: python
  category: compatibility
  description: multiprocessing.Process.kill() is only available on Python 3.7+ and
    is therefore not backwards compatible. Instead, use terminate().
  patterns:
  - multiprocessing.Process.kill()
  source_rules:
  - python37-compatibility-multiprocess2
  tier: T2
- id: EXT.PY.COMPAT.034
  name: Kill
  ecosystem: python
  category: compatibility
  description: multiprocessing.Process.kill() is only available on Python 3.7+ and
    is therefore not backwards compatible. Instead, use terminate().
  patterns:
  - multiprocessing.Process.kill()
  source_rules:
  - python37-compatibility-multiprocess2
  tier: T2
- id: EXT.PY.COMPAT.035
  name: Os Preadv
  ecosystem: python
  category: compatibility
  description: os.preadv() is only available on Python 3.7+ and is therefore not backwards
    compatible. Instead, use a combination of os.readv() and os.pread().
  patterns:
  - os.preadv(...)
  source_rules:
  - python37-compatibility-os1
  tier: T2
- id: EXT.PY.COMPAT.036
  name: Preadv
  ecosystem: python
  category: compatibility
  description: os.preadv() is only available on Python 3.7+ and is therefore not backwards
    compatible. Instead, use a combination of os.readv() and os.pread().
  patterns:
  - os.preadv(...)
  source_rules:
  - python37-compatibility-os1
  tier: T2
- id: EXT.PY.COMPAT.037
  name: Os Pwritev
  ecosystem: python
  category: compatibility
  description: os.pwritev() is only available on Python 3.3+ and is therefore not
    backwards compatible. Instead, use a combination of pwrite() and writev().
  patterns:
  - os.pwritev(...)
  source_rules:
  - python37-compatibility-os2-ok2
  tier: T2
- id: EXT.PY.COMPAT.038
  name: Pwritev
  ecosystem: python
  category: compatibility
  description: os.pwritev() is only available on Python 3.3+ and is therefore not
    backwards compatible. Instead, use a combination of pwrite() and writev().
  patterns:
  - os.pwritev(...)
  source_rules:
  - python37-compatibility-os2-ok2
  tier: T2
- id: EXT.PY.BEST.039
  name: Pdb Set Trace
  ecosystem: python
  category: best-practice
  description: pdb.set_trace() with the header argument is only available on Python
    3.7+ and is therefore not backwards compatible. Instead, use set_trace() without
    the header argument.
  patterns:
  - pdb.set_trace()
  - pdb.set_trace(header=$X, ...)
  source_rules:
  - python37-compatibility-pdb
  - python-debugger-found
  tier: T2
- id: EXT.PY.BEST.040
  name: Set Trace
  ecosystem: python
  category: best-practice
  description: pdb.set_trace() with the header argument is only available on Python
    3.7+ and is therefore not backwards compatible. Instead, use set_trace() without
    the header argument.
  patterns:
  - pdb.set_trace()
  - pdb.set_trace(header=$X, ...)
  source_rules:
  - python37-compatibility-pdb
  - python-debugger-found
  tier: T2
- id: EXT.PY.COMPAT.041
  name: Ssl Get Ciphers
  ecosystem: python
  category: compatibility
  description: this function is only available on Python 3.6+
  patterns:
  - ssl.get_ciphers()
  source_rules:
  - python36-compatibility-ssl
  tier: T2
- id: EXT.PY.COMPAT.042
  name: Get Ciphers
  ecosystem: python
  category: compatibility
  description: this function is only available on Python 3.6+
  patterns:
  - ssl.get_ciphers()
  source_rules:
  - python36-compatibility-ssl
  tier: T2
- id: EXT.PY.COMPAT.043
  name: Subprocess Popen
  ecosystem: python
  category: compatibility
  description: the `errors` argument to Popen is only available on Python 3.6+
  patterns:
  - subprocess.Popen($ARGS, ...)
  - subprocess.Popen(encoding=$X, ...)
  - subprocess.Popen(errors=$X, ...)
  source_rules:
  - subprocess-list-passed-as-string
  - python36-compatibility-Popen1
  - python36-compatibility-Popen2
  tier: T2
- id: EXT.PY.SEC.044
  name: Popen
  ecosystem: python
  category: security
  description: the `errors` argument to Popen is only available on Python 3.6+
  patterns:
  - os.popen(...)
  - subprocess.Popen($ARGS, ...)
  - os.popen($CMD,...)
  - subprocess.Popen(errors=$X, ...)
  - subprocess.Popen(encoding=$X, ...)
  source_rules:
  - subprocess-list-passed-as-string
  - python36-compatibility-Popen1
  - dangerous-system-call
  - python36-compatibility-Popen2
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.045
  name: Ssl Create Unverified Context
  ecosystem: python
  category: security
  description: Unverified SSL context detected. This will permit insecure connections
    without verifying SSL certificates. Use 'ssl.create_default_context' instead.
  patterns:
  - ssl._create_unverified_context(...)
  - ssl._create_default_https_context = ssl._create_unverified_context
  source_rules:
  - unverified-ssl-context
  tier: T2
- id: EXT.PY.SEC.046
  name: Create Unverified Context
  ecosystem: python
  category: security
  description: Unverified SSL context detected. This will permit insecure connections
    without verifying SSL certificates. Use 'ssl.create_default_context' instead.
  patterns:
  - ssl._create_unverified_context(...)
  source_rules:
  - unverified-ssl-context
  tier: T2
- id: EXT.PY.SEC.047
  name: Ssl Create Default Https Context
  ecosystem: python
  category: security
  description: Unverified SSL context detected. This will permit insecure connections
    without verifying SSL certificates. Use 'ssl.create_default_context' instead.
  patterns:
  - ssl._create_default_https_context = ssl._create_unverified_context
  source_rules:
  - unverified-ssl-context
  tier: T2
- id: EXT.PY.SEC.048
  name: Csv Writer
  ecosystem: python
  category: security
  description: Detected the generation of a CSV file using the built-in `csv` module.
    If user data is used to generate the data in this file, it is possible that an
    attacker could inject a formula when the CSV is im
  patterns:
  - csv.writer(...)
  source_rules:
  - use-defusedcsv
  tier: T2
- id: EXT.PY.SEC.049
  name: Writer
  ecosystem: python
  category: security
  description: Detected the generation of a CSV file using the built-in `csv` module.
    If user data is used to generate the data in this file, it is possible that an
    attacker could inject a formula when the CSV is im
  patterns:
  - csv.writer(...)
  source_rules:
  - use-defusedcsv
  tier: T2
- id: EXT.PY.SEC.050
  name: Xml Etree Elementtree Parse
  ecosystem: python
  category: security
  description: The native Python `xml` library is vulnerable to XML External Entity
    (XXE) attacks.  These attacks can leak confidential data and "XML bombs" can cause
    denial of service. Do not use this library to pa
  patterns:
  - xml.etree.ElementTree.parse($...ARGS)
  source_rules:
  - use-defused-xml-parse
  tier: T2
- id: EXT.PY.SEC.051
  name: Parse
  ecosystem: python
  category: security
  description: The native Python `xml` library is vulnerable to XML External Entity
    (XXE) attacks.  These attacks can leak confidential data and "XML bombs" can cause
    denial of service. Do not use this library to pa
  patterns:
  - xml.etree.ElementTree.parse($...ARGS)
  source_rules:
  - use-defused-xml-parse
  tier: T2
- id: EXT.PY.SEC.052
  name: Request Form Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.form.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.053
  name: Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.headers.get(...)
  - $REQ.QUERY_PARAMS.get(...)
  - flask.request.form.get(...)
  - flask.request.values.get(...)
  - $REQ.query_params.get(...)
  source_rules:
  - dangerous-interactive-code-run
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-os-exec
  - dangerous-globals-use
  - csv-writer-injection
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-testcapi-run-in-subinterp
  - dangerous-os-exec-tainted-env-args
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.054
  name: Request Form
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.form[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.055
  name: Request Args Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.args.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.056
  name: Request Args
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.args[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.057
  name: Request Values Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.values.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.058
  name: Request Values
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.values[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.059
  name: Request Cookies Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.cookies.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.060
  name: Request Cookies
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.cookies[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.061
  name: Request Stream
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.stream
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.062
  name: Request Headers Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.headers.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.063
  name: Request Headers
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.headers[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.064
  name: Request Data
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.data
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.065
  name: Request Full Path
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.full_path
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.066
  name: Request Url
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.url
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.067
  name: Request Json
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.json
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.068
  name: Request Get Json
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.get_json()
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.069
  name: Get Json
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.get_json()
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.070
  name: Request View Args Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.view_args.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.071
  name: Request View Args
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - flask.request.view_args[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - csv-writer-injection
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.072
  name: Property Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - request.$PROPERTY.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.073
  name: Req Post Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.POST.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.074
  name: Req Post
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.POST[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.075
  name: Req Files Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.FILES.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.076
  name: Req Files
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.FILES[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.077
  name: Req Data Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.DATA.get(...)
  - $REQ.data.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.078
  name: Req Data
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.data[...]
  - $REQ.DATA[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.079
  name: Req Query Params Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.query_params.get(...)
  - $REQ.QUERY_PARAMS.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.080
  name: Req Query Params
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.QUERY_PARAMS[...]
  - $REQ.query_params[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.081
  name: Req Content Type
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.content_type
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.082
  name: Req Stream
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - $REQ.stream
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.083
  name: Self Requestline
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - self.requestline
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.084
  name: Self Path
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - self.path
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.085
  name: Self Headers
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - self.headers[...]
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.086
  name: Self Headers Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - self.headers.get(...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.087
  name: Self Rfile
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - self.rfile
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-interactive-code-run
  - dangerous-spawn-process
  - dangerous-system-call
  - dangerous-os-exec
  - dangerous-testcapi-run-in-subinterp
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.088
  name: Hashlib Sha1
  ecosystem: python
  category: security
  description: Detected SHA1 hash algorithm which is considered insecure. SHA1 is
    not collision resistant and is therefore not suitable as a cryptographic signature.
    Use SHA256 or SHA3 instead.
  patterns:
  - hashlib.sha1(...)
  source_rules:
  - insecure-hash-algorithm-sha1
  tier: T2
- id: EXT.PY.SEC.089
  name: Sha1
  ecosystem: python
  category: security
  description: Detected SHA1 hash algorithm which is considered insecure. SHA1 is
    not collision resistant and is therefore not suitable as a cryptographic signature.
    Use SHA256 or SHA3 instead.
  patterns:
  - hashlib.sha1(...)
  source_rules:
  - insecure-hash-algorithm-sha1
  tier: T2
- id: EXT.PY.SEC.090
  name: G Get
  ecosystem: python
  category: security
  description: Found non static data as an index to 'globals()'. This is extremely
    dangerous because it allows an attacker to execute arbitrary code on the system.
    Refactor your code not to use 'globals()'.
  patterns:
  - $G.get(...)
  source_rules:
  - dangerous-globals-use
  tier: T2
- id: EXT.PY.SEC.091
  name: Func Globals
  ecosystem: python
  category: security
  description: Found non static data as an index to 'globals()'. This is extremely
    dangerous because it allows an attacker to execute arbitrary code on the system.
    Refactor your code not to use 'globals()'.
  patterns:
  - $FUNC.__globals__[...]
  source_rules:
  - dangerous-globals-use
  tier: T2
- id: EXT.PY.SEC.092
  name: Xxsubinterpreters Run String
  ecosystem: python
  category: security
  description: Found user controlled content in `run_string`. This is dangerous because
    it allows a malicious actor to run arbitrary Python code.
  patterns:
  - _xxsubinterpreters.run_string($ID, $PAYLOAD, ...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-subinterpreters-run-string-audit
  tier: T2
- id: EXT.PY.SEC.093
  name: Run String
  ecosystem: python
  category: security
  description: Found user controlled content in `run_string`. This is dangerous because
    it allows a malicious actor to run arbitrary Python code.
  patterns:
  - _xxsubinterpreters.run_string($ID, $PAYLOAD, ...)
  source_rules:
  - dangerous-subinterpreters-run-string
  - dangerous-subinterpreters-run-string-audit
  tier: T2
- id: EXT.PY.SEC.094
  name: Uuid Uuid1
  ecosystem: python
  category: security
  description: Using UUID version 1 for UUID generation can lead to predictable UUIDs
    based on system information (e.g., MAC address, timestamp). This may lead to security
    risks such as the sandwich attack. Consider
  patterns:
  - uuid.uuid1(...)
  source_rules:
  - insecure-uuid-version
  tier: T2
- id: EXT.PY.SEC.095
  name: Uuid1
  ecosystem: python
  category: security
  description: Using UUID version 1 for UUID generation can lead to predictable UUIDs
    based on system information (e.g., MAC address, timestamp). This may lead to security
    risks such as the sandwich attack. Consider
  patterns:
  - uuid.uuid1(...)
  source_rules:
  - insecure-uuid-version
  tier: T2
- id: EXT.PY.SEC.096
  name: Os System
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.system(...)
  - os.system($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.097
  name: System
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - __import__("os").system(...)
  - os.system(...)
  - os.system($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.098
  name: Os Popen
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen(...)
  - os.popen($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.099
  name: Os Popen2
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen2(...)
  - os.popen2($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.100
  name: Popen2
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen2(...)
  - os.popen2($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.101
  name: Os Popen3
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen3(...)
  - os.popen3($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.102
  name: Popen3
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen3(...)
  - os.popen3($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.103
  name: Os Popen4
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen4(...)
  - os.popen4($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.104
  name: Popen4
  ecosystem: python
  category: security
  description: Found user-controlled data used in a system call. This could allow
    a malicious actor to execute commands. Use the 'subprocess' module instead, which
    is easier to use without accidentally exposing a co
  patterns:
  - os.popen4(...)
  - os.popen4($CMD,...)
  source_rules:
  - dangerous-system-call
  - dangerous-system-call-tainted-env-args
  - dangerous-system-call-audit
  tier: T2
- id: EXT.PY.SEC.105
  name: Os Environ
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.environ['$ANYTHING']
  - os.environ
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.106
  name: Os Environ Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.environ.get('$FOO', ...)
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.107
  name: Os Environb
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.environb['$ANYTHING']
  - os.environb
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.108
  name: Os Environb Get
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.environb.get('$FOO', ...)
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.109
  name: Os Getenv
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.getenv('$ANYTHING', ...)
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.110
  name: Getenv
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.getenv('$ANYTHING', ...)
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.111
  name: Os Getenvb
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.getenvb('$ANYTHING', ...)
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.112
  name: Getenvb
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - os.getenvb('$ANYTHING', ...)
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.113
  name: Sys Argv
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - sys.argv
  - sys.argv[...]
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.114
  name: Sys Orig Argv
  ecosystem: python
  category: security
  description: Found user controlled content when spawning a process. This is dangerous
    because it allows a malicious actor to execute commands.
  patterns:
  - sys.orig_argv[...]
  - sys.orig_argv
  source_rules:
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-spawn-process
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-asyncio-exec-tainted-env-args
  - dangerous-testcapi-run-in-subinterp-tainted-env-args
  - dangerous-interactive-code-run-tainted-env-args
  - dangerous-system-call-tainted-env-args
  - dangerous-os-exec-tainted-env-args
  - dangerous-asyncio-shell-tainted-env-args
  - dangerous-subinterpreters-run-string-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.115
  name: Testcapi Run In Subinterp
  ecosystem: python
  category: security
  description: Found user controlled content in `run_in_subinterp`. This is dangerous
    because it allows a malicious actor to run arbitrary Python code.
  patterns:
  - _testcapi.run_in_subinterp($PAYLOAD, ...)
  source_rules:
  - dangerous-testcapi-run-in-subinterp-audit
  - dangerous-testcapi-run-in-subinterp
  tier: T2
- id: EXT.PY.SEC.116
  name: Run In Subinterp
  ecosystem: python
  category: security
  description: Found user controlled content in `run_in_subinterp`. This is dangerous
    because it allows a malicious actor to run arbitrary Python code.
  patterns:
  - _testcapi.run_in_subinterp($PAYLOAD, ...)
  - test.support.run_in_subinterp($PAYLOAD, ...)
  source_rules:
  - dangerous-testcapi-run-in-subinterp-audit
  - dangerous-testcapi-run-in-subinterp
  tier: T2
- id: EXT.PY.SEC.117
  name: Test Support Run In Subinterp
  ecosystem: python
  category: security
  description: Found user controlled content in `run_in_subinterp`. This is dangerous
    because it allows a malicious actor to run arbitrary Python code.
  patterns:
  - test.support.run_in_subinterp($PAYLOAD, ...)
  source_rules:
  - dangerous-testcapi-run-in-subinterp-audit
  - dangerous-testcapi-run-in-subinterp
  tier: T2
- id: EXT.PY.SEC.118
  name: X Push
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.push($PAYLOAD,...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.119
  name: Push
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.push($PAYLOAD,...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.120
  name: X Runsource
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.runsource($PAYLOAD,...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.121
  name: Runsource
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.runsource($PAYLOAD,...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.122
  name: X Runcode
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.runcode(code.compile_command($PAYLOAD),...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.123
  name: Code Compile Command
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.runcode(code.compile_command($PAYLOAD),...)
  - $PL = code.compile_command($PAYLOAD,...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.124
  name: Runcode
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.runcode(code.compile_command($PAYLOAD),...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.125
  name: Compile Command
  ecosystem: python
  category: security
  description: Found user controlled data inside InteractiveConsole/InteractiveInterpreter
    method. This is dangerous if external data can reach this function call because
    it allows a malicious actor to run arbitrary
  patterns:
  - $X.runcode(code.compile_command($PAYLOAD),...)
  - $PL = code.compile_command($PAYLOAD,...)
  source_rules:
  - dangerous-interactive-code-run-audit
  - dangerous-interactive-code-run
  tier: T2
- id: EXT.PY.SEC.126
  name: Hashlib Md5
  ecosystem: python
  category: security
  description: Detected MD5 hash algorithm which is considered insecure. MD5 is not
    collision resistant and is therefore not suitable as a cryptographic signature.
    Use SHA256 or SHA3 instead.
  patterns:
  - hashlib.md5(...)
  source_rules:
  - insecure-hash-algorithm-md5
  tier: T2
- id: EXT.PY.SEC.127
  name: Md5
  ecosystem: python
  category: security
  description: Detected MD5 hash algorithm which is considered insecure. MD5 is not
    collision resistant and is therefore not suitable as a cryptographic signature.
    Use SHA256 or SHA3 instead.
  patterns:
  - hashlib.md5(...)
  source_rules:
  - insecure-hash-algorithm-md5
  tier: T2
- id: EXT.PY.SEC.128
  name: Hashlib New
  ecosystem: python
  category: security
  description: Detected use of an insecure MD4 or MD5 hash function. These functions
    have known vulnerabilities and are considered deprecated. Consider using 'SHA256'
    or a similar function instead.
  patterns:
  - hashlib.new(..., name="=~/[M|m][D|d][4|5]/", ...)
  - hashlib.new("=~/[M|m][D|d][4|5]/", ...)
  source_rules:
  - insecure-hash-function
  tier: T2
- id: EXT.PY.SEC.129
  name: New
  ecosystem: python
  category: security
  description: Detected use of an insecure MD4 or MD5 hash function. These functions
    have known vulnerabilities and are considered deprecated. Consider using 'SHA256'
    or a similar function instead.
  patterns:
  - hashlib.new(..., name="=~/[M|m][D|d][4|5]/", ...)
  - hashlib.new("=~/[M|m][D|d][4|5]/", ...)
  - AES.new("",...)
  source_rules:
  - insecure-hash-function
  - empty-aes-key
  tier: T2
- id: EXT.PY.CORR.130
  name: Fd Write
  ecosystem: python
  category: correctness
  description: The file object '$FD' was opened in read mode, but is being written
    to. This will cause a runtime error.
  patterns:
  - $FD.write(...)
  source_rules:
  - writing-to-file-in-read-mode
  tier: T2
- id: EXT.PY.CORR.131
  name: Write
  ecosystem: python
  category: correctness
  description: The file object '$FD' was opened in read mode, but is being written
    to. This will cause a runtime error.
  patterns:
  - $FD.write(...)
  source_rules:
  - writing-to-file-in-read-mode
  tier: T2
- id: EXT.PY.BEST.132
  name: Time Sleep
  ecosystem: python
  category: best-practice
  description: Synchronous time.sleep in async code will block the event loop and
    not allow other tasks to execute. Use asyncio.sleep() instead.
  patterns:
  - 'time.sleep($X: float)'
  - time.sleep(...)
  - 'time.sleep($X: int)'
  source_rules:
  - arbitrary-sleep
  - sync-sleep-in-async-code
  tier: T2
- id: EXT.PY.BEST.133
  name: Sleep
  ecosystem: python
  category: best-practice
  description: Synchronous time.sleep in async code will block the event loop and
    not allow other tasks to execute. Use asyncio.sleep() instead.
  patterns:
  - 'time.sleep($X: float)'
  - time.sleep(...)
  - 'time.sleep($X: int)'
  source_rules:
  - arbitrary-sleep
  - sync-sleep-in-async-code
  tier: T2
- id: EXT.PY.CORR.134
  name: Snapshot Assert Match
  ecosystem: python
  category: correctness
  description: snapshot.assert_match makes use of pathlib to create files. Patching
    $METHOD may result in unexpected snapshot behavior
  patterns:
  - snapshot.assert_match(...)
  source_rules:
  - pytest-assert_match-after-path-patch
  tier: T2
- id: EXT.PY.CORR.135
  name: Assert Match
  ecosystem: python
  category: correctness
  description: snapshot.assert_match makes use of pathlib to create files. Patching
    $METHOD may result in unexpected snapshot behavior
  patterns:
  - snapshot.assert_match(...)
  source_rules:
  - pytest-assert_match-after-path-patch
  tier: T2
- id: EXT.PY.CORR.136
  name: Pdb Pdb
  ecosystem: python
  category: correctness
  description: pdb is an interactive debugging tool and you may have forgotten to
    remove it before committing your code
  patterns:
  - pdb.Pdb.$X(...)
  source_rules:
  - pdb-remove
  tier: T2
- id: EXT.PY.CORR.137
  name: Executor Map
  ecosystem: python
  category: correctness
  description: 'Values returned by thread pool map must be read in order to raise
    exceptions. Consider using `for _ in $EXECUTOR.map(...): pass`.'
  patterns:
  - $EXECUTOR.map(...)
  source_rules:
  - uncaught-executor-exceptions
  tier: T2
- id: EXT.PY.CORR.138
  name: Map
  ecosystem: python
  category: correctness
  description: 'Values returned by thread pool map must be read in order to raise
    exceptions. Consider using `for _ in $EXECUTOR.map(...): pass`.'
  patterns:
  - $EXECUTOR.map(...)
  source_rules:
  - uncaught-executor-exceptions
  tier: T2
- id: EXT.PY.CORR.139
  name: Functools Lru Cache
  ecosystem: python
  category: correctness
  description: Generators can only be consumed once, so in most cases, caching them
    will cause an error when the already-consumed generator is retrieved from cache.
  patterns:
  - functools.lru_cache(...)
  source_rules:
  - cannot-cache-generators
  tier: T2
- id: EXT.PY.CORR.140
  name: Lru Cache
  ecosystem: python
  category: correctness
  description: Generators can only be consumed once, so in most cases, caching them
    will cause an error when the already-consumed generator is retrieved from cache.
  patterns:
  - functools.lru_cache(...)
  source_rules:
  - cannot-cache-generators
  tier: T2
- id: EXT.PY.CORR.141
  name: ', $A,  , $B, '
  ecosystem: python
  category: correctness
  description: Class $C inherits from both `$A` and `$B` which both have a method
    named `$F`; one of these methods will be overwritten.
  patterns:
  - 'class $C(..., $A, ..., $B, ...):'
  source_rules:
  - baseclass-attribute-override
  tier: T2
- id: EXT.PY.CORR.142
  name: Dict Items
  ecosystem: python
  category: correctness
  description: 'It appears that `$DICT[$KEY]` is a dict with items being deleted while
    in a for loop. This is usually a bad idea and will likely lead to a RuntimeError:
    dictionary changed size during iteration'
  patterns:
  - 'for $KEY, $VALUE in $DICT.items():'
  source_rules:
  - dict-del-while-iterate
  tier: T2
- id: EXT.PY.CORR.143
  name: Items
  ecosystem: python
  category: correctness
  description: 'It appears that `$DICT[$KEY]` is a dict with items being deleted while
    in a for loop. This is usually a bad idea and will likely lead to a RuntimeError:
    dictionary changed size during iteration'
  patterns:
  - 'for $KEY, $VALUE in $DICT.items():'
  source_rules:
  - dict-del-while-iterate
  tier: T2
- id: EXT.PY.CORR.144
  name: Dict Keys
  ecosystem: python
  category: correctness
  description: 'It appears that `$DICT[$KEY]` is a dict with items being deleted while
    in a for loop. This is usually a bad idea and will likely lead to a RuntimeError:
    dictionary changed size during iteration'
  patterns:
  - 'for $KEY in $DICT.keys():'
  source_rules:
  - dict-del-while-iterate
  tier: T2
- id: EXT.PY.CORR.145
  name: Keys
  ecosystem: python
  category: correctness
  description: 'It appears that `$DICT[$KEY]` is a dict with items being deleted while
    in a for loop. This is usually a bad idea and will likely lead to a RuntimeError:
    dictionary changed size during iteration'
  patterns:
  - 'for $KEY in $DICT.keys():'
  source_rules:
  - dict-del-while-iterate
  tier: T2
- id: EXT.PY.BEST.146
  name: '...'
  ecosystem: python
  category: best-practice
  description: 'Class `$A` has defined `__eq__` which means it should also have defined
    `__hash__`; '
  patterns:
  - 'class A(...):'
  source_rules:
  - missing-hash-with-eq
  tier: T2
- id: EXT.PY.BEST.147
  name: Io Open
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = io.open(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.148
  name: Open
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = io.open(...)
  - $F = tarfile.open(...)
  - shelve.open($SINK,...)
  - $F = ZipFile.open(...)
  - $OPENER.open(...)
  source_rules:
  - dynamic-urllib-use-detected
  - tainted-pickle-deserialization
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.149
  name: Tarfile Open
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tarfile.open(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.150
  name: Zipfile Open
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = ZipFile.open(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.151
  name: Tempfile Temporaryfile
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tempfile.TemporaryFile(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.152
  name: Temporaryfile
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tempfile.TemporaryFile(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.153
  name: Tempfile Namedtemporaryfile
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tempfile.NamedTemporaryFile(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.154
  name: Namedtemporaryfile
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tempfile.NamedTemporaryFile(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.155
  name: Tempfile Spooledtemporaryfile
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tempfile.SpooledTemporaryFile(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.BEST.156
  name: Spooledtemporaryfile
  ecosystem: python
  category: best-practice
  description: file object opened without corresponding close
  patterns:
  - $F = tempfile.SpooledTemporaryFile(...)
  source_rules:
  - open-never-closed
  tier: T2
- id: EXT.PY.CORR.157
  name: Tempfile Mktemp
  ecosystem: python
  category: correctness
  description: 'Use tempfile.NamedTemporaryFile instead. From the official Python
    documentation: THIS FUNCTION IS UNSAFE AND SHOULD NOT BE USED. The file name may
    refer to a file that did not exist at some point, but'
  patterns:
  - tempfile.mktemp(...)
  source_rules:
  - tempfile-insecure
  tier: T2
- id: EXT.PY.CORR.158
  name: Mktemp
  ecosystem: python
  category: correctness
  description: 'Use tempfile.NamedTemporaryFile instead. From the official Python
    documentation: THIS FUNCTION IS UNSAFE AND SHOULD NOT BE USED. The file name may
    refer to a file that did not exist at some point, but'
  patterns:
  - tempfile.mktemp(...)
  source_rules:
  - tempfile-insecure
  tier: T2
- id: EXT.PY.CORR.159
  name: F Name
  ecosystem: python
  category: correctness
  description: Using '$F.name' without '.flush()' or '.close()' may cause an error
    because the file may not exist when '$F.name' is used. Use '.flush()' or close
    the file before using '$F.name'.
  patterns:
  - $F.name
  source_rules:
  - tempfile-without-flush
  tier: T2
- id: EXT.PY.CORR.160
  name: D Update
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default dict $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.update(...)
  source_rules:
  - default-mutable-dict
  tier: T2
- id: EXT.PY.CORR.161
  name: Update
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default dict $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.update(...)
  source_rules:
  - default-mutable-dict
  tier: T2
- id: EXT.PY.CORR.162
  name: D Setdefault
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default dict $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.setdefault(...)
  source_rules:
  - default-mutable-dict
  tier: T2
- id: EXT.PY.CORR.163
  name: Setdefault
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default dict $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.setdefault(...)
  source_rules:
  - default-mutable-dict
  tier: T2
- id: EXT.PY.CORR.164
  name: D Append
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default list $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.append(...)
  source_rules:
  - default-mutable-list
  tier: T2
- id: EXT.PY.CORR.165
  name: Append
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default list $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.append(...)
  source_rules:
  - default-mutable-list
  tier: T2
- id: EXT.PY.CORR.166
  name: D Extend
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default list $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.extend(...)
  source_rules:
  - default-mutable-list
  tier: T2
- id: EXT.PY.CORR.167
  name: Extend
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default list $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.extend(...)
  source_rules:
  - default-mutable-list
  tier: T2
- id: EXT.PY.CORR.168
  name: D Insert
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default list $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.insert(...)
  source_rules:
  - default-mutable-list
  tier: T2
- id: EXT.PY.CORR.169
  name: Insert
  ecosystem: python
  category: correctness
  description: 'Function $F mutates default list $D. Python only instantiates default
    function arguments once and shares the instance across the function calls. If
    the default function argument is mutated, that will '
  patterns:
  - $D.insert(...)
  source_rules:
  - default-mutable-list
  tier: T2
- id: EXT.PY.SEC.170
  name: Loop Subprocess Shell
  ecosystem: python
  category: security
  description: Detected asyncio subprocess function without a static string. If this
    data can be controlled by a malicious actor, it may be an instance of command
    injection. Audit the use of this call to ensure it i
  patterns:
  - $LOOP.subprocess_shell($PROTOCOL, $CMD)
  source_rules:
  - dangerous-asyncio-shell
  - dangerous-asyncio-shell-audit
  tier: T2
- id: EXT.PY.SEC.171
  name: Subprocess Shell
  ecosystem: python
  category: security
  description: Detected asyncio subprocess function without a static string. If this
    data can be controlled by a malicious actor, it may be an instance of command
    injection. Audit the use of this call to ensure it i
  patterns:
  - $LOOP.subprocess_shell($PROTOCOL, $CMD)
  source_rules:
  - dangerous-asyncio-shell
  - dangerous-asyncio-shell-audit
  tier: T2
- id: EXT.PY.SEC.172
  name: Asyncio Subprocess Create Subprocess Shell
  ecosystem: python
  category: security
  description: Detected asyncio subprocess function without a static string. If this
    data can be controlled by a malicious actor, it may be an instance of command
    injection. Audit the use of this call to ensure it i
  patterns:
  - asyncio.subprocess.create_subprocess_shell($CMD, ...)
  source_rules:
  - dangerous-asyncio-shell
  - dangerous-asyncio-shell-audit
  tier: T2
- id: EXT.PY.SEC.173
  name: Create Subprocess Shell
  ecosystem: python
  category: security
  description: Detected asyncio subprocess function without a static string. If this
    data can be controlled by a malicious actor, it may be an instance of command
    injection. Audit the use of this call to ensure it i
  patterns:
  - asyncio.subprocess.create_subprocess_shell($CMD, ...)
  - asyncio.create_subprocess_shell($CMD, ...)
  source_rules:
  - dangerous-asyncio-shell
  - dangerous-asyncio-shell-audit
  tier: T2
- id: EXT.PY.SEC.174
  name: Asyncio Create Subprocess Shell
  ecosystem: python
  category: security
  description: Detected asyncio subprocess function without a static string. If this
    data can be controlled by a malicious actor, it may be an instance of command
    injection. Audit the use of this call to ensure it i
  patterns:
  - asyncio.create_subprocess_shell($CMD, ...)
  source_rules:
  - dangerous-asyncio-shell
  - dangerous-asyncio-shell-audit
  tier: T2
- id: EXT.PY.SEC.175
  name: Importlib Import Module
  ecosystem: python
  category: security
  description: Untrusted user input in `importlib.import_module()` function allows
    an attacker to load arbitrary code. Avoid dynamic values in `importlib.import_module()`
    or use a whitelist to prevent running untrus
  patterns:
  - importlib.import_module($NAME, ...)
  source_rules:
  - non-literal-import
  tier: T2
- id: EXT.PY.SEC.176
  name: Import Module
  ecosystem: python
  category: security
  description: Untrusted user input in `importlib.import_module()` function allows
    an attacker to load arbitrary code. Avoid dynamic values in `importlib.import_module()`
    or use a whitelist to prevent running untrus
  patterns:
  - importlib.import_module($NAME, ...)
  source_rules:
  - non-literal-import
  tier: T2
- id: EXT.PY.SEC.177
  name: Join
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - '" ".join($LIST)'
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.178
  name: Subprocess Run
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.run($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.179
  name: Run
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.run($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.180
  name: Subprocess Call
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.call(["$BINPATH",...],...)
  - subprocess.call($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  - python-reverse-shell
  tier: T2
- id: EXT.PY.SEC.181
  name: Call
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.call(["$BINPATH",...],...)
  - subprocess.call($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  - python-reverse-shell
  tier: T2
- id: EXT.PY.SEC.182
  name: Subprocess Check Call
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.check_call($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.183
  name: Check Call
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.check_call($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.184
  name: Subprocess Check Output
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.check_output($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.185
  name: Check Output
  ecosystem: python
  category: security
  description: Detected `" ".join(...)` being passed to `subprocess.run`. This can
    lead to argument splitting issues and potential security vulnerabilities. Instead,
    pass the list directly to `subprocess.run` to pre
  patterns:
  - subprocess.check_output($ARGS, ...)
  source_rules:
  - subprocess-list-passed-as-string
  tier: T2
- id: EXT.PY.SEC.186
  name: Asyncio Create Subprocess Exec
  ecosystem: python
  category: security
  description: Detected 'create_subprocess_exec' function without a static string.
    If this data can be controlled by a malicious actor, it may be an instance of
    command injection. Audit the use of this call to ensur
  patterns:
  - asyncio.create_subprocess_exec($PROG, [$CMD, ...], ...)
  - asyncio.create_subprocess_exec(...)
  - asyncio.create_subprocess_exec($PROG, "=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c",...)
  - asyncio.create_subprocess_exec($PROG, ["=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c",
    $CMD, ...], ...)
  - asyncio.create_subprocess_exec($PROG, $CMD, ...)
  source_rules:
  - dangerous-asyncio-create-exec
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-asyncio-create-exec-audit
  tier: T2
- id: EXT.PY.SEC.187
  name: Create Subprocess Exec
  ecosystem: python
  category: security
  description: Detected 'create_subprocess_exec' function without a static string.
    If this data can be controlled by a malicious actor, it may be an instance of
    command injection. Audit the use of this call to ensur
  patterns:
  - asyncio.subprocess.create_subprocess_exec(...)
  - asyncio.create_subprocess_exec(...)
  - asyncio.create_subprocess_exec($PROG, "=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c",...)
  - asyncio.create_subprocess_exec($PROG, [$CMD, ...], ...)
  - asyncio.subprocess.create_subprocess_exec($PROG, "=~/(sh|bash|ksh|csh|tcsh|zsh)/",
    "-c", $CMD, ...)
  source_rules:
  - dangerous-asyncio-create-exec
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-asyncio-create-exec-audit
  tier: T2
- id: EXT.PY.SEC.188
  name: Asyncio Subprocess Create Subprocess Exec
  ecosystem: python
  category: security
  description: Detected 'create_subprocess_exec' function without a static string.
    If this data can be controlled by a malicious actor, it may be an instance of
    command injection. Audit the use of this call to ensur
  patterns:
  - asyncio.subprocess.create_subprocess_exec(...)
  - asyncio.subprocess.create_subprocess_exec($PROG, "=~/(sh|bash|ksh|csh|tcsh|zsh)/",
    "-c", $CMD, ...)
  - asyncio.subprocess.create_subprocess_exec($PROG, [$CMD, ...], ...)
  - asyncio.subprocess.create_subprocess_exec($PROG, $CMD, ...)
  - asyncio.subprocess.create_subprocess_exec($PROG, ["=~/(sh|bash|ksh|csh|tcsh|zsh)/",
    "-c", $CMD, ...], ...)
  source_rules:
  - dangerous-asyncio-create-exec
  - dangerous-asyncio-create-exec-tainted-env-args
  - dangerous-asyncio-create-exec-audit
  tier: T2
- id: EXT.PY.SEC.189
  name: Marshal Dump
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - marshal.dump(...)
  source_rules:
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.190
  name: Dump
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - marshal.dump(...)
  source_rules:
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.191
  name: Marshal Dumps
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - marshal.dumps(...)
  source_rules:
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.192
  name: Dumps
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - marshal.dumps(...)
  source_rules:
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.193
  name: Marshal Load
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - marshal.load(...)
  source_rules:
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.194
  name: Load
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - cPickle.load($SINK,...)
  - _pickle.load($SINK,...)
  - pickle.load($SINK,...)
  - marshal.load(...)
  - dill.load($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.195
  name: Marshal Loads
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - marshal.loads(...)
  source_rules:
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.196
  name: Loads
  ecosystem: python
  category: security
  description: 'The marshal module is not intended to be secure against erroneous
    or maliciously constructed data. Never unmarshal data received from an untrusted
    or unauthenticated source. See more details: https://'
  patterns:
  - cPickle.loads($SINK,...)
  - dill.loads($SINK,...)
  - _pickle.loads($SINK,...)
  - marshal.loads(...)
  - pickle.loads($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  - marshal-usage
  tier: T2
- id: EXT.PY.SEC.197
  name: Pty Spawn
  ecosystem: python
  category: security
  description: Semgrep found a Python reverse shell using $BINPATH to $IP at $PORT
  patterns:
  - pty.spawn("$BINPATH",...)
  source_rules:
  - python-reverse-shell
  tier: T2
- id: EXT.PY.SEC.198
  name: Spawn
  ecosystem: python
  category: security
  description: Semgrep found a Python reverse shell using $BINPATH to $IP at $PORT
  patterns:
  - pty.spawn("$BINPATH",...)
  source_rules:
  - python-reverse-shell
  tier: T2
- id: EXT.PY.SEC.199
  name: C Annotations
  ecosystem: python
  category: security
  description: Annotations passed to `typing.get_type_hints` are evaluated in `globals`
    and `locals` namespaces. Make sure that no arbitrary value can be written as the
    annotation and passed to `typing.get_type_hint
  patterns:
  - $C.__annotations__[$NAME] = $X
  source_rules:
  - dangerous-annotations-usage
  tier: T2
- id: EXT.PY.SEC.200
  name: Db Execute
  ecosystem: python
  category: security
  description: Detected possible formatted SQL query. Use parameterized queries instead.
  patterns:
  - $DB.execute($SQL)
  - $DB.execute(f"...")
  - $DB.execute("...".format(...))
  - $DB.execute("..." % ...)
  source_rules:
  - formatted-sql-query
  tier: T2
- id: EXT.PY.SEC.201
  name: Execute
  ecosystem: python
  category: security
  description: Detected possible formatted SQL query. Use parameterized queries instead.
  patterns:
  - $DB.execute($SQL)
  - $DB.execute(f"...")
  - $DB.execute("...".format(...))
  - $DB.execute("..." % ...)
  source_rules:
  - formatted-sql-query
  tier: T2
- id: EXT.PY.SEC.202
  name: Ssl Protocol Sslv2
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - ssl.PROTOCOL_SSLv2
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.203
  name: Ssl Protocol Sslv3
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - ssl.PROTOCOL_SSLv3
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.204
  name: Ssl Protocol Tlsv1
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - ssl.PROTOCOL_TLSv1
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.205
  name: Ssl Protocol Tlsv1 1
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - ssl.PROTOCOL_TLSv1_1
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.206
  name: Pyopenssl Ssl Sslv2 Method
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - pyOpenSSL.SSL.SSLv2_METHOD
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.207
  name: Pyopenssl Ssl Sslv23 Method
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - pyOpenSSL.SSL.SSLv23_METHOD
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.208
  name: Pyopenssl Ssl Sslv3 Method
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - pyOpenSSL.SSL.SSLv3_METHOD
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.209
  name: Pyopenssl Ssl Tlsv1 Method
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - pyOpenSSL.SSL.TLSv1_METHOD
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.210
  name: Pyopenssl Ssl Tlsv1 1 Method
  ecosystem: python
  category: security
  description: An insecure SSL version was detected. TLS versions 1.0, 1.1, and all
    SSL versions are considered weak encryption and are deprecated. Use 'ssl.PROTOCOL_TLSv1_2'
    or higher.
  patterns:
  - pyOpenSSL.SSL.TLSv1_1_METHOD
  source_rules:
  - weak-ssl-version
  tier: T2
- id: EXT.PY.SEC.211
  name: Httplib Httpsconnection
  ecosystem: python
  category: security
  description: The HTTPSConnection API has changed frequently with minor releases
    of Python. Ensure you are using the API for your version of Python securely. For
    example, Python 3 versions prior to 3.4.3 will not v
  patterns:
  - httplib.HTTPSConnection(...)
  source_rules:
  - httpsconnection-detected
  tier: T2
- id: EXT.PY.SEC.212
  name: Six Moves Http Client Httpsconnection
  ecosystem: python
  category: security
  description: The HTTPSConnection API has changed frequently with minor releases
    of Python. Ensure you are using the API for your version of Python securely. For
    example, Python 3 versions prior to 3.4.3 will not v
  patterns:
  - six.moves.http_client.HTTPSConnection(...)
  source_rules:
  - httpsconnection-detected
  tier: T2
- id: EXT.PY.SEC.213
  name: Shlex Quote
  ecosystem: python
  category: security
  description: Detected subprocess function '$FUNC' with user controlled data. A malicious
    actor could leverage this to perform command injection. You may consider using
    'shlex.quote()'.
  patterns:
  - shlex.quote(...)
  source_rules:
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.214
  name: Quote
  ecosystem: python
  category: security
  description: Detected subprocess function '$FUNC' with user controlled data. A malicious
    actor could leverage this to perform command injection. You may consider using
    'shlex.quote()'.
  patterns:
  - shlex.quote(...)
  - pipes.quote(...)
  source_rules:
  - dangerous-subprocess-use-tainted-env-args
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.215
  name: Stat S Iwgrp
  ecosystem: python
  category: security
  description: These permissions `$BITS` are widely permissive and grant access to
    more people than may be necessary. A good default is `0o644` which gives read
    and write access to yourself and read access to everyo
  patterns:
  - <... stat.S_IWGRP ...>
  source_rules:
  - insecure-file-permissions
  tier: T2
- id: EXT.PY.SEC.216
  name: Stat S Ixgrp
  ecosystem: python
  category: security
  description: These permissions `$BITS` are widely permissive and grant access to
    more people than may be necessary. A good default is `0o644` which gives read
    and write access to yourself and read access to everyo
  patterns:
  - <... stat.S_IXGRP ...>
  source_rules:
  - insecure-file-permissions
  tier: T2
- id: EXT.PY.SEC.217
  name: Stat S Iwoth
  ecosystem: python
  category: security
  description: These permissions `$BITS` are widely permissive and grant access to
    more people than may be necessary. A good default is `0o644` which gives read
    and write access to yourself and read access to everyo
  patterns:
  - <... stat.S_IWOTH ...>
  source_rules:
  - insecure-file-permissions
  tier: T2
- id: EXT.PY.SEC.218
  name: Stat S Ixoth
  ecosystem: python
  category: security
  description: These permissions `$BITS` are widely permissive and grant access to
    more people than may be necessary. A good default is `0o644` which gives read
    and write access to yourself and read access to everyo
  patterns:
  - <... stat.S_IXOTH ...>
  source_rules:
  - insecure-file-permissions
  tier: T2
- id: EXT.PY.SEC.219
  name: Stat S Irwxo
  ecosystem: python
  category: security
  description: These permissions `$BITS` are widely permissive and grant access to
    more people than may be necessary. A good default is `0o644` which gives read
    and write access to yourself and read access to everyo
  patterns:
  - <... stat.S_IRWXO ...>
  source_rules:
  - insecure-file-permissions
  tier: T2
- id: EXT.PY.SEC.220
  name: Stat S Irwxg
  ecosystem: python
  category: security
  description: These permissions `$BITS` are widely permissive and grant access to
    more people than may be necessary. A good default is `0o644` which gives read
    and write access to yourself and read access to everyo
  patterns:
  - <... stat.S_IRWXG ...>
  source_rules:
  - insecure-file-permissions
  tier: T2
- id: EXT.PY.SEC.221
  name: Ssl Wrap Socket
  ecosystem: python
  category: security
  description: '''ssl.wrap_socket()'' is deprecated. This function creates an insecure
    socket without server name indication or hostname matching. Instead, create an
    SSL context using ''ssl.SSLContext()'' and use that to'
  patterns:
  - ssl.wrap_socket(..., cert_reqs=$REQS, ...)
  - ssl.wrap_socket(...)
  source_rules:
  - ssl-wrap-socket-is-deprecated
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.222
  name: Wrap Socket
  ecosystem: python
  category: security
  description: '''ssl.wrap_socket()'' is deprecated. This function creates an insecure
    socket without server name indication or hostname matching. Instead, create an
    SSL context using ''ssl.SSLContext()'' and use that to'
  patterns:
  - $CONTEXT.wrap_socket(..., cert_reqs=$REQS, ...)
  - ssl.wrap_socket(..., cert_reqs=$REQS, ...)
  - ssl.wrap_socket(...)
  source_rules:
  - ssl-wrap-socket-is-deprecated
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.223
  name: Loop Subprocess Exec
  ecosystem: python
  category: security
  description: Detected subprocess function '$LOOP.subprocess_exec' with user controlled
    data. You may consider using 'shlex.escape()'.
  patterns:
  - $LOOP.subprocess_exec($PROTOCOL, "=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c",...)
  - $LOOP.subprocess_exec($PROTOCOL, ["=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c", $CMD,
    ...], ...)
  - $LOOP.subprocess_exec($PROTOCOL, ["=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c", ...],
    ...)
  - $LOOP.subprocess_exec(...)
  - $LOOP.subprocess_exec($PROTOCOL, "=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c", $CMD,
    ...)
  source_rules:
  - dangerous-asyncio-exec-audit
  - dangerous-asyncio-exec
  - dangerous-asyncio-exec-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.224
  name: Subprocess Exec
  ecosystem: python
  category: security
  description: Detected subprocess function '$LOOP.subprocess_exec' with user controlled
    data. You may consider using 'shlex.escape()'.
  patterns:
  - $LOOP.subprocess_exec($PROTOCOL, "=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c",...)
  - $LOOP.subprocess_exec($PROTOCOL, ["=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c", $CMD,
    ...], ...)
  - $LOOP.subprocess_exec($PROTOCOL, ["=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c", ...],
    ...)
  - $LOOP.subprocess_exec(...)
  - $LOOP.subprocess_exec($PROTOCOL, "=~/(sh|bash|ksh|csh|tcsh|zsh)/", "-c", $CMD,
    ...)
  source_rules:
  - dangerous-asyncio-exec-audit
  - dangerous-asyncio-exec
  - dangerous-asyncio-exec-tainted-env-args
  tier: T2
- id: EXT.PY.SEC.225
  name: Re Compile
  ecosystem: python
  category: security
  description: Detected usage of re.compile with an inefficient regular expression.
    This can lead to regular expression denial of service, which can result in service
    down time. Instead, check all regexes or use saf
  patterns:
  - $A = re.compile("$B", ...)
  source_rules:
  - regex_dos
  tier: T2
- id: EXT.PY.SEC.226
  name: Compile
  ecosystem: python
  category: security
  description: Detected usage of re.compile with an inefficient regular expression.
    This can lead to regular expression denial of service, which can result in service
    down time. Instead, check all regexes or use saf
  patterns:
  - $A = re.compile("$B", ...)
  source_rules:
  - regex_dos
  tier: T2
- id: EXT.PY.SEC.227
  name: Hashlib Sha224
  ecosystem: python
  category: security
  description: This code uses a 224-bit hash function, which is deprecated or disallowed
    in some security policies. Consider updating to a stronger hash function such
    as SHA-384 or higher to ensure compliance and se
  patterns:
  - hashlib.sha224(...)
  source_rules:
  - sha224-hash
  tier: T2
- id: EXT.PY.SEC.228
  name: Sha224
  ecosystem: python
  category: security
  description: This code uses a 224-bit hash function, which is deprecated or disallowed
    in some security policies. Consider updating to a stronger hash function such
    as SHA-384 or higher to ensure compliance and se
  patterns:
  - hashlib.sha224(...)
  source_rules:
  - sha224-hash
  tier: T2
- id: EXT.PY.SEC.229
  name: Hashlib Sha3 224
  ecosystem: python
  category: security
  description: This code uses a 224-bit hash function, which is deprecated or disallowed
    in some security policies. Consider updating to a stronger hash function such
    as SHA-384 or higher to ensure compliance and se
  patterns:
  - hashlib.sha3_224(...)
  source_rules:
  - sha224-hash
  tier: T2
- id: EXT.PY.SEC.230
  name: Sha3 224
  ecosystem: python
  category: security
  description: This code uses a 224-bit hash function, which is deprecated or disallowed
    in some security policies. Consider updating to a stronger hash function such
    as SHA-384 or higher to ensure compliance and se
  patterns:
  - hashlib.sha3_224(...)
  source_rules:
  - sha224-hash
  tier: T2
- id: EXT.PY.SEC.231
  name: Multiprocessing Connection Connection Recv
  ecosystem: python
  category: security
  description: The Connection.recv() method automatically unpickles the data it receives,
    which can be a security risk unless you can trust the process which sent the message.
    Therefore, unless the connection object
  patterns:
  - multiprocessing.connection.Connection.recv(...)
  source_rules:
  - multiprocessing-recv
  tier: T2
- id: EXT.PY.SEC.232
  name: Recv
  ecosystem: python
  category: security
  description: The Connection.recv() method automatically unpickles the data it receives,
    which can be a security risk unless you can trust the process which sent the message.
    Therefore, unless the connection object
  patterns:
  - multiprocessing.connection.Client.recv(...)
  - multiprocessing.connection.Connection.recv(...)
  source_rules:
  - multiprocessing-recv
  tier: T2
- id: EXT.PY.SEC.233
  name: Multiprocessing Connection Client Recv
  ecosystem: python
  category: security
  description: The Connection.recv() method automatically unpickles the data it receives,
    which can be a security risk unless you can trust the process which sent the message.
    Therefore, unless the connection object
  patterns:
  - multiprocessing.connection.Client.recv(...)
  source_rules:
  - multiprocessing-recv
  tier: T2
- id: EXT.PY.SEC.234
  name: Multiprocessing Connection Client
  ecosystem: python
  category: security
  description: The Connection.recv() method automatically unpickles the data it receives,
    which can be a security risk unless you can trust the process which sent the message.
    Therefore, unless the connection object
  patterns:
  - $C = multiprocessing.connection.Client(...)
  source_rules:
  - multiprocessing-recv
  tier: T2
- id: EXT.PY.SEC.235
  name: Client
  ecosystem: python
  category: security
  description: The Connection.recv() method automatically unpickles the data it receives,
    which can be a security risk unless you can trust the process which sent the message.
    Therefore, unless the connection object
  patterns:
  - $C = multiprocessing.connection.Client(...)
  source_rules:
  - multiprocessing-recv
  tier: T2
- id: EXT.PY.SEC.236
  name: Urllib Urlopen
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - urllib.urlopen(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.237
  name: Urlopen
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - urllib.request.urlopen(...)
  - urllib.urlopen(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.238
  name: Urllib Request Urlopen
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - urllib.request.urlopen(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.239
  name: Urllib Urlretrieve
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - urllib.urlretrieve(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.240
  name: Urlretrieve
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - urllib.urlretrieve(...)
  - urllib.request.urlretrieve(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.241
  name: Urllib Request Urlretrieve
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - urllib.request.urlretrieve(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.242
  name: Opener Open
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - $OPENER.open(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.243
  name: Opener Retrieve
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - $OPENER.retrieve(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.244
  name: Retrieve
  ecosystem: python
  category: security
  description: Detected a dynamic value being used with urllib. urllib supports 'file://'
    schemes, so a dynamic value controlled by a malicious actor may allow them to
    read arbitrary files. Audit uses of urllib call
  patterns:
  - $OPENER.retrieve(...)
  source_rules:
  - dynamic-urllib-use-detected
  tier: T2
- id: EXT.PY.SEC.245
  name: Urllib3 Httpconnectionpool
  ecosystem: python
  category: security
  description: Detected HTTPConnectionPool. This will transmit data in cleartext.
    It is recommended to use HTTPSConnectionPool instead for to encrypt communications.
  patterns:
  - urllib3.HTTPConnectionPool(...)
  source_rules:
  - http-not-https-connection
  tier: T2
- id: EXT.PY.SEC.246
  name: Httpconnectionpool
  ecosystem: python
  category: security
  description: Detected HTTPConnectionPool. This will transmit data in cleartext.
    It is recommended to use HTTPSConnectionPool instead for to encrypt communications.
  patterns:
  - urllib3.HTTPConnectionPool(...)
  - urllib3.connectionpool.HTTPConnectionPool(...)
  source_rules:
  - http-not-https-connection
  tier: T2
- id: EXT.PY.SEC.247
  name: Urllib3 Connectionpool Httpconnectionpool
  ecosystem: python
  category: security
  description: Detected HTTPConnectionPool. This will transmit data in cleartext.
    It is recommended to use HTTPSConnectionPool instead for to encrypt communications.
  patterns:
  - urllib3.connectionpool.HTTPConnectionPool(...)
  source_rules:
  - http-not-https-connection
  tier: T2
- id: EXT.PY.SEC.248
  name: Urllib3 Poolmanager
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.PoolManager(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.249
  name: Poolmanager
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.PoolManager(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.250
  name: Urllib3 Proxymanager
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.ProxyManager(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.251
  name: Proxymanager
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.ProxyManager(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.252
  name: Urllib3 Httpsconnectionpool
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.HTTPSConnectionPool(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.253
  name: Httpsconnectionpool
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.connectionpool.HTTPSConnectionPool(..., cert_reqs=$REQS, ...)
  - urllib3.HTTPSConnectionPool(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.254
  name: Urllib3 Connectionpool Httpsconnectionpool
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.connectionpool.HTTPSConnectionPool(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.255
  name: Urllib3 Connection From Url
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.connection_from_url(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.256
  name: Connection From Url
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.connection_from_url(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.257
  name: Urllib3 Proxy From Url
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.proxy_from_url(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.258
  name: Proxy From Url
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - urllib3.proxy_from_url(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.259
  name: Context Wrap Socket
  ecosystem: python
  category: security
  description: certificate verification explicitly disabled, insecure connections
    possible
  patterns:
  - $CONTEXT.wrap_socket(..., cert_reqs=$REQS, ...)
  source_rules:
  - disabled-cert-validation
  tier: T2
- id: EXT.PY.SEC.260
  name: Socket Socket
  ecosystem: python
  category: security
  description: Running `socket.bind` to 0.0.0.0, or empty string could unexpectedly
    expose the server publicly as it binds to all available interfaces. Consider instead
    getting correct address from an environment va
  patterns:
  - $S = socket.socket(...)
  source_rules:
  - avoid-bind-to-all-interfaces
  tier: T2
- id: EXT.PY.SEC.261
  name: Socket
  ecosystem: python
  category: security
  description: Running `socket.bind` to 0.0.0.0, or empty string could unexpectedly
    expose the server publicly as it binds to all available interfaces. Consider instead
    getting correct address from an environment va
  patterns:
  - $S = socket.socket(...)
  source_rules:
  - avoid-bind-to-all-interfaces
  tier: T2
- id: EXT.PY.SEC.262
  name: Logging Config Listen
  ecosystem: python
  category: security
  description: Because portions of the logging configuration are passed through eval(),
    use of this function may open its users to a security risk. While the function
    only binds to a socket on localhost, and so does
  patterns:
  - logging.config.listen(...)
  source_rules:
  - listen-eval
  tier: T2
- id: EXT.PY.SEC.263
  name: Listen
  ecosystem: python
  category: security
  description: Because portions of the logging configuration are passed through eval(),
    use of this function may open its users to a security risk. While the function
    only binds to a socket on localhost, and so does
  patterns:
  - logging.config.listen(...)
  source_rules:
  - listen-eval
  tier: T2
- id: EXT.PY.SEC.264
  name: Table Scan
  ecosystem: python
  category: security
  description: Detected DynamoDB query filter that is tainted by `$EVENT` object.
    This could lead to NoSQL injection if the variable is user-controlled and not
    properly sanitized. Explicitly assign query params inst
  patterns:
  - $TABLE.scan(..., ScanFilter = $SINK, ...)
  source_rules:
  - dynamodb-filter-injection
  tier: T2
- id: EXT.PY.SEC.265
  name: Scan
  ecosystem: python
  category: security
  description: Detected DynamoDB query filter that is tainted by `$EVENT` object.
    This could lead to NoSQL injection if the variable is user-controlled and not
    properly sanitized. Explicitly assign query params inst
  patterns:
  - $TABLE.scan(..., ScanFilter = $SINK, ...)
  source_rules:
  - dynamodb-filter-injection
  tier: T2
- id: EXT.PY.SEC.266
  name: Table Query
  ecosystem: python
  category: security
  description: Detected DynamoDB query filter that is tainted by `$EVENT` object.
    This could lead to NoSQL injection if the variable is user-controlled and not
    properly sanitized. Explicitly assign query params inst
  patterns:
  - $TABLE.query(..., QueryFilter = $SINK, ...)
  source_rules:
  - dynamodb-filter-injection
  tier: T2
- id: EXT.PY.SEC.267
  name: Query
  ecosystem: python
  category: security
  description: Detected DynamoDB query filter that is tainted by `$EVENT` object.
    This could lead to NoSQL injection if the variable is user-controlled and not
    properly sanitized. Explicitly assign query params inst
  patterns:
  - $TABLE.query(..., QueryFilter = $SINK, ...)
  source_rules:
  - dynamodb-filter-injection
  tier: T2
- id: EXT.PY.SEC.268
  name: Pickle Load
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - pickle.load($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.269
  name: Pickle Loads
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - pickle.loads($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.270
  name: Pickle Load
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - _pickle.load($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.271
  name: Pickle Loads
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - _pickle.loads($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.272
  name: Cpickle Load
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - cPickle.load($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.273
  name: Cpickle Loads
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - cPickle.loads($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.274
  name: Dill Load
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - dill.load($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.275
  name: Dill Loads
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - dill.loads($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.276
  name: Shelve Open
  ecosystem: python
  category: security
  description: Avoid using `pickle`, which is known to lead to code execution vulnerabilities.
    When unpickling, the serialized data could be manipulated to run arbitrary code.
    Instead, consider serializing the relev
  patterns:
  - shelve.open($SINK,...)
  source_rules:
  - tainted-pickle-deserialization
  tier: T2
- id: EXT.PY.SEC.277
  name: Shlex Split
  ecosystem: python
  category: security
  description: Detected subprocess function with argument tainted by an `event` object.  If
    this data can be controlled by a malicious actor, it may be an instance of command
    injection. The default option for `shell
  patterns:
  - shlex.split(...)
  source_rules:
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.278
  name: Split
  ecosystem: python
  category: security
  description: Detected subprocess function with argument tainted by an `event` object.  If
    this data can be controlled by a malicious actor, it may be an instance of command
    injection. The default option for `shell
  patterns:
  - shlex.split(...)
  source_rules:
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.279
  name: Pipes Quote
  ecosystem: python
  category: security
  description: Detected subprocess function with argument tainted by an `event` object.  If
    this data can be controlled by a malicious actor, it may be an instance of command
    injection. The default option for `shell
  patterns:
  - pipes.quote(...)
  source_rules:
  - dangerous-subprocess-use
  tier: T2
- id: EXT.PY.SEC.280
  name: Aes New
  ecosystem: python
  category: security
  description: 'Potential empty AES encryption key. Using an empty key in AES encryption
    can result in weak encryption and may allow attackers to easily decrypt sensitive
    data. Ensure that a strong, non-empty key is '
  patterns:
  - AES.new("",...)
  source_rules:
  - empty-aes-key
  tier: T2
- id: EXT.PY.SEC.281
  name: Resp Set Cookie
  ecosystem: python
  category: security
  description: Found a Flask cookie with insecurely configured properties.  By default
    the secure, httponly and samesite ar configured insecurely. cookies should be
    handled securely by setting `secure=True`, `httpon
  patterns:
  - $RESP.set_cookie(...)
  source_rules:
  - secure-set-cookie
  tier: T2
- id: EXT.PY.SEC.282
  name: Set Cookie
  ecosystem: python
  category: security
  description: Found a Flask cookie with insecurely configured properties.  By default
    the secure, httponly and samesite ar configured insecurely. cookies should be
    handled securely by setting `secure=True`, `httpon
  patterns:
  - $RESP.set_cookie(...)
  source_rules:
  - secure-set-cookie
  tier: T2
- id: EXT.DJANGO.COMPAT.001
  name: Dispatch Signals Signal Disconnect
  ecosystem: django
  category: compatibility
  description: The weak argument to django.dispatch.signals.Signal.disconnect() is
    removed in Django 2.0.
  patterns:
  - django.dispatch.signals.Signal.disconnect(..., weak=$X, ...)
  source_rules:
  - django-compat-2_0-signals-weak
  tier: T2
- id: EXT.DJANGO.COMPAT.002
  name: Disconnect
  ecosystem: django
  category: compatibility
  description: The weak argument to django.dispatch.signals.Signal.disconnect() is
    removed in Django 2.0.
  patterns:
  - django.dispatch.signals.Signal.disconnect(..., weak=$X, ...)
  source_rules:
  - django-compat-2_0-signals-weak
  tier: T2
- id: EXT.DJANGO.COMPAT.003
  name: Db Backends Base Basedatabaseoperations Check Aggregate Support
  ecosystem: django
  category: compatibility
  description: django.db.backends.base.BaseDatabaseOperations.check_aggregate_support()
    is removed in Django 2.0.
  patterns:
  - django.db.backends.base.BaseDatabaseOperations.check_aggregate_support(...)
  source_rules:
  - django-compat-2_0-check-aggregate-support
  tier: T2
- id: EXT.DJANGO.COMPAT.004
  name: Check Aggregate Support
  ecosystem: django
  category: compatibility
  description: django.db.backends.base.BaseDatabaseOperations.check_aggregate_support()
    is removed in Django 2.0.
  patterns:
  - django.db.backends.base.BaseDatabaseOperations.check_aggregate_support(...)
  source_rules:
  - django-compat-2_0-check-aggregate-support
  tier: T2
- id: EXT.DJANGO.COMPAT.005
  name: Forms
  ecosystem: django
  category: compatibility
  description: The django.forms.extras package is removed in Django 2.0.
  patterns:
  - from django.forms import extras as $Y
  - from django.forms import extras
  source_rules:
  - django-compat-2_0-extra-forms
  tier: T2
- id: EXT.DJANGO.COMPAT.006
  name: Forms Extras
  ecosystem: django
  category: compatibility
  description: The django.forms.extras package is removed in Django 2.0.
  patterns:
  - from django.forms.extras import $X as $Y
  - from django.forms.extras import $X
  - import django.forms.extras
  - import django.forms.extras.$X
  - import django.forms.extras.$X as $Y
  source_rules:
  - django-compat-2_0-extra-forms
  tier: T2
- id: EXT.DJANGO.COMPAT.007
  name: X Assignment Tag
  ecosystem: django
  category: compatibility
  description: The assignment_tag helper is removed in Django 2.0.
  patterns:
  - $X.assignment_tag(...)
  source_rules:
  - django-compat-2_0-assignment-tag
  tier: T2
- id: EXT.DJANGO.COMPAT.008
  name: Assignment Tag
  ecosystem: django
  category: compatibility
  description: The assignment_tag helper is removed in Django 2.0.
  patterns:
  - $X.assignment_tag(...)
  source_rules:
  - django-compat-2_0-assignment-tag
  tier: T2
- id: EXT.DJANGO.COMPAT.009
  name: X Assertredirects
  ecosystem: django
  category: compatibility
  description: The host argument to assertRedirects is removed in Django 2.0.
  patterns:
  - $X.assertRedirects(..., host=$Y, ...)
  source_rules:
  - django-compat-2_0-assert-redirects-helper
  tier: T2
- id: EXT.DJANGO.COMPAT.010
  name: Assertredirects
  ecosystem: django
  category: compatibility
  description: The host argument to assertRedirects is removed in Django 2.0.
  patterns:
  - $X.assertRedirects(..., host=$Y, ...)
  source_rules:
  - django-compat-2_0-assert-redirects-helper
  tier: T2
- id: EXT.DJANGO.SEC.011
  name: Hashids Hashids
  ecosystem: django
  category: security
  description: The Django secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Django secret ke
  patterns:
  - hashids.Hashids(..., salt=django.conf.settings.SECRET_KEY, ...)
  - hashids.Hashids(django.conf.settings.SECRET_KEY, ...)
  source_rules:
  - hashids-with-django-secret
  tier: T2
- id: EXT.DJANGO.SEC.012
  name: Conf Settings Secret Key
  ecosystem: django
  category: security
  description: The Django secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Django secret ke
  patterns:
  - hashids.Hashids(..., salt=django.conf.settings.SECRET_KEY, ...)
  - hashids.Hashids(django.conf.settings.SECRET_KEY, ...)
  source_rules:
  - hashids-with-django-secret
  tier: T2
- id: EXT.DJANGO.SEC.013
  name: Hashids
  ecosystem: django
  category: security
  description: The Django secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Django secret ke
  patterns:
  - hashids.Hashids(..., salt=django.conf.settings.SECRET_KEY, ...)
  - hashids.Hashids(django.conf.settings.SECRET_KEY, ...)
  source_rules:
  - hashids-with-django-secret
  tier: T2
- id: EXT.DJANGO.SEC.014
  name: Property Get
  ecosystem: django
  category: security
  description: Found user input going directly into typecast for bool(), float(),
    or complex(). This allows an attacker to inject Python's not-a-number (NaN) into
    the typecast. This results in undefind behavior, par
  patterns:
  - request.$PROPERTY.get(...)
  source_rules:
  - nan-injection
  tier: T2
- id: EXT.DJANGO.SEC.015
  name: Get
  ecosystem: django
  category: security
  description: Found user input going directly into typecast for bool(), float(),
    or complex(). This allows an attacker to inject Python's not-a-number (NaN) into
    the typecast. This results in undefind behavior, par
  patterns:
  - requests.$METHOD(..., f"...{request.$W.get(...)}...", ...)
  - $V = request.$W.get($X)
  - os.system(..., $S % request.$W.get(...), ...)
  - $MODEL.objects.extra(..., where=[..., f"...{request.$W.get(...)}...", ...], ...)
  - django.shortcuts.redirect(..., request.$W.get(...), ...)
  source_rules:
  - avoid-insecure-deserialization
  - user-eval
  - user-eval-format-string
  - path-traversal-open
  - path-traversal-join
  - sql-injection-using-extra-where
  - use-none-for-password-default
  - reflected-data-httpresponse
  - path-traversal-file-name
  - reflected-data-httpresponsebadrequest
  tier: T2
- id: EXT.DJANGO.SEC.016
  name: Request Post
  ecosystem: django
  category: security
  description: Use $FORM.cleaned_data[] instead of request.POST[] after form.is_valid()
    has been executed to only access sanitized data
  patterns:
  - request.POST[...]
  source_rules:
  - django-using-request-post-after-is-valid
  tier: T2
- id: EXT.DJANGO.SEC.017
  name: Request Post Get
  ecosystem: django
  category: security
  description: Use $FORM.cleaned_data[] instead of request.POST[] after form.is_valid()
    has been executed to only access sanitized data
  patterns:
  - request.POST.get(...)
  source_rules:
  - django-using-request-post-after-is-valid
  tier: T2
- id: EXT.DJANGO.SEC.018
  name: Shortcuts Render
  ecosystem: django
  category: security
  description: Using 'locals()' as a context to 'render(...)' is extremely dangerous.
    This exposes Python functions to the template that were not meant to be exposed.
    An attacker could use these functions to execute
  patterns:
  - django.shortcuts.render(..., locals(...), ...)
  - django.shortcuts.render(..., globals(...), ...)
  - django.shortcuts.render(..., $CONTEXT, ...)
  source_rules:
  - globals-as-template-context
  - locals-as-template-context
  tier: T2
- id: EXT.DJANGO.SEC.019
  name: Render
  ecosystem: django
  category: security
  description: Using 'locals()' as a context to 'render(...)' is extremely dangerous.
    This exposes Python functions to the template that were not meant to be exposed.
    An attacker could use these functions to execute
  patterns:
  - django.template.Template.render(..., globals(...), ...)
  - django.shortcuts.render(..., globals(...), ...)
  - django.template.Template.render(..., $CONTEXT, ...)
  - django.template.Template.render(..., locals(...), ...)
  - django.shortcuts.render(..., locals(...), ...)
  source_rules:
  - globals-as-template-context
  - locals-as-template-context
  tier: T2
- id: EXT.DJANGO.SEC.020
  name: Template Template Render
  ecosystem: django
  category: security
  description: Using 'locals()' as a context to 'render(...)' is extremely dangerous.
    This exposes Python functions to the template that were not meant to be exposed.
    An attacker could use these functions to execute
  patterns:
  - django.template.Template.render(..., locals(...), ...)
  - django.template.Template.render(..., globals(...), ...)
  - django.template.Template.render(..., $CONTEXT, ...)
  source_rules:
  - globals-as-template-context
  - locals-as-template-context
  tier: T2
- id: EXT.DJANGO.MAINT.021
  name: Urls Path
  ecosystem: django
  category: maintainability
  description: path for `$URL` is uselessly assigned twice
  patterns:
  - '[..., django.urls.path(''$URL'', $VIEW, ...), ..., django.urls.path(''$URL'',
    $DIFFERENT_VIEW, ...), ...]'
  - '[..., django.urls.path(''$URL'', $VIEW, name=''$NAME'', ...), ..., django.urls.path(''$URL'',
    $VIEW, name=''$OTHER_NAME'', ...), ...]'
  - '[..., django.urls.path(''$URL'', $VIEW, name=''$NAME'', ...), ..., django.urls.path(''$OTHER_URL'',
    $OTHER_VIEW, name=''$NAME'', ...), ...]'
  - '[..., django.urls.path(''$URL'', $VIEW, ...), ..., django.urls.path(''$URL'',
    $VIEW, ...), ...]'
  source_rules:
  - duplicate-path-assignment
  - duplicate-path-assignment-different-names
  - duplicate-name-assignment
  - conflicting-path-assignment
  tier: T2
- id: EXT.DJANGO.MAINT.022
  name: Path
  ecosystem: django
  category: maintainability
  description: path for `$URL` is uselessly assigned twice
  patterns:
  - '[..., django.urls.path(''$URL'', $VIEW, ...), ..., django.urls.path(''$URL'',
    $DIFFERENT_VIEW, ...), ...]'
  - '[..., django.urls.path(''$URL'', $VIEW, name=''$NAME'', ...), ..., django.urls.path(''$URL'',
    $VIEW, name=''$OTHER_NAME'', ...), ...]'
  - '[..., django.urls.path(''$URL'', $VIEW, name=''$NAME'', ...), ..., django.urls.path(''$OTHER_URL'',
    $OTHER_VIEW, name=''$NAME'', ...), ...]'
  - '[..., django.urls.path(''$URL'', $VIEW, ...), ..., django.urls.path(''$URL'',
    $VIEW, ...), ...]'
  source_rules:
  - duplicate-path-assignment
  - duplicate-path-assignment-different-names
  - duplicate-name-assignment
  - conflicting-path-assignment
  tier: T2
- id: EXT.DJANGO.CORR.023
  name: Db Models Charfield
  ecosystem: django
  category: correctness
  description: 'Avoid using null on string-based fields such as CharField and TextField.
    If a string-based field has null=True, that means it has two possible values for
    "no data": NULL, and the empty string. In most'
  patterns:
  - $F = django.db.models.CharField(..., null=True, ...)
  - $F = django.db.models.CharField(..., unique=True, blank=True, ...)
  source_rules:
  - no-null-string-field
  - string-field-must-set-null-true
  tier: T2
- id: EXT.DJANGO.CORR.024
  name: Charfield
  ecosystem: django
  category: correctness
  description: 'Avoid using null on string-based fields such as CharField and TextField.
    If a string-based field has null=True, that means it has two possible values for
    "no data": NULL, and the empty string. In most'
  patterns:
  - $F = django.db.models.CharField(..., null=True, ...)
  - $F = django.db.models.CharField(..., unique=True, blank=True, ...)
  source_rules:
  - no-null-string-field
  - string-field-must-set-null-true
  tier: T2
- id: EXT.DJANGO.CORR.025
  name: Db Models Textfield
  ecosystem: django
  category: correctness
  description: 'Avoid using null on string-based fields such as CharField and TextField.
    If a string-based field has null=True, that means it has two possible values for
    "no data": NULL, and the empty string. In most'
  patterns:
  - $F = django.db.models.TextField(..., unique=True, blank=True, ...)
  - $F = django.db.models.TextField(..., null=True, ...)
  source_rules:
  - no-null-string-field
  - string-field-must-set-null-true
  tier: T2
- id: EXT.DJANGO.CORR.026
  name: Textfield
  ecosystem: django
  category: correctness
  description: 'Avoid using null on string-based fields such as CharField and TextField.
    If a string-based field has null=True, that means it has two possible values for
    "no data": NULL, and the empty string. In most'
  patterns:
  - $F = django.db.models.TextField(..., unique=True, blank=True, ...)
  - $F = django.db.models.TextField(..., null=True, ...)
  source_rules:
  - no-null-string-field
  - string-field-must-set-null-true
  tier: T2
- id: EXT.DJANGO.CORR.027
  name: Db Models Floatfield
  ecosystem: django
  category: correctness
  description: Found a FloatField used for variable $F. Use DecimalField for currency
    fields to avoid float-rounding errors.
  patterns:
  - $F = django.db.models.FloatField(...)
  source_rules:
  - use-decimalfield-for-money
  tier: T2
- id: EXT.DJANGO.CORR.028
  name: Floatfield
  ecosystem: django
  category: correctness
  description: Found a FloatField used for variable $F. Use DecimalField for currency
    fields to avoid float-rounding errors.
  patterns:
  - $F = django.db.models.FloatField(...)
  source_rules:
  - use-decimalfield-for-money
  tier: T2
- id: EXT.DJANGO.PERF.029
  name: X User Id
  ecosystem: django
  category: performance
  description: You should use ITEM.user_id rather than ITEM.user.id to prevent running
    an extra query.
  patterns:
  - $X.user.id
  source_rules:
  - access-foreign-keys
  tier: T2
- id: EXT.DJANGO.PERF.030
  name: X Objects Order By
  ecosystem: django
  category: performance
  description: Looks like you are only accessing first element of an ordered QuerySet.
    Use `latest()` or `earliest()` instead. See https://docs.djangoproject.com/en/3.0/ref/models/querysets/#django.db.models.query.Q
  patterns:
  - $X.objects.order_by(...)[0]
  source_rules:
  - use-earliest-or-latest
  tier: T2
- id: EXT.DJANGO.PERF.031
  name: Order By
  ecosystem: django
  category: performance
  description: Looks like you are only accessing first element of an ordered QuerySet.
    Use `latest()` or `earliest()` instead. See https://docs.djangoproject.com/en/3.0/ref/models/querysets/#django.db.models.query.Q
  patterns:
  - $X.objects.$FUNC(...).$FILTER(...).order_by(...)[0]
  - $X.objects.$FUNC(...).order_by(...)[0]
  - $X.objects.order_by(...)[0]
  source_rules:
  - use-earliest-or-latest
  tier: T2
- id: EXT.DJANGO.PERF.032
  name: X Objects
  ecosystem: django
  category: performance
  description: Looks like you are only accessing first element of an ordered QuerySet.
    Use `latest()` or `earliest()` instead. See https://docs.djangoproject.com/en/3.0/ref/models/querysets/#django.db.models.query.Q
  patterns:
  - $X.objects.$FUNC(...).order_by(...)[0]
  - $X.objects.$FUNC(...).$FILTER().$UPDATE(...).len()
  - $X.objects.$FUNC(...).len()
  - $X.objects.$FUNC(...).$FILTER().len()
  - $X.objects.$FUNC(...).$FILTER(...).order_by(...)[0]
  source_rules:
  - use-earliest-or-latest
  - use-count-method
  tier: T2
- id: EXT.DJANGO.PERF.033
  name: Len
  ecosystem: django
  category: performance
  description: Looks like you need to determine the number of records. Django provides
    the count() method which is more efficient than .len(). See https://docs.djangoproject.com/en/3.0/ref/models/querysets/
  patterns:
  - $X.objects.$FUNC(...).$FILTER().$UPDATE(...).len()
  - $X.objects.$FUNC(...).$FILTER().len()
  - $X.objects.$FUNC(...).len()
  source_rules:
  - use-count-method
  tier: T2
- id: EXT.DJANGO.BEST.034
  name: Db Models Foreignkey
  ecosystem: django
  category: best-practice
  description: Use 'django.db.models.OneToOneField' instead of 'ForeignKey' with unique=True.
    'OneToOneField' is used to create one-to-one relationships.
  patterns:
  - $F = django.db.models.ForeignKey(..., unique=True, ...)
  source_rules:
  - use-onetoonefield
  tier: T2
- id: EXT.DJANGO.BEST.035
  name: Foreignkey
  ecosystem: django
  category: best-practice
  description: Use 'django.db.models.OneToOneField' instead of 'ForeignKey' with unique=True.
    'OneToOneField' is used to create one-to-one relationships.
  patterns:
  - $F = django.db.models.ForeignKey(..., unique=True, ...)
  source_rules:
  - use-onetoonefield
  tier: T2
- id: EXT.DJANGO.BEST.036
  name: Json Dumps
  ecosystem: django
  category: best-practice
  description: Use JsonResponse instead
  patterns:
  - $Y = json.dumps(...)
  source_rules:
  - use-json-response
  tier: T2
- id: EXT.DJANGO.BEST.037
  name: Dumps
  ecosystem: django
  category: best-practice
  description: Use JsonResponse instead
  patterns:
  - $Y = json.dumps(...)
  source_rules:
  - use-json-response
  tier: T2
- id: EXT.DJANGO.SEC.038
  name: Model Set Password
  ecosystem: django
  category: security
  description: '''$VAR'' is the empty string and is being used to set the password
    on ''$MODEL''. If you meant to set an unusable password, set the password to None
    or call ''set_unusable_password()''.'
  patterns:
  - $MODEL.set_password($EMPTY)
  - $MODEL.set_password($X)
  source_rules:
  - unvalidated-password
  - password-empty-string
  tier: T2
- id: EXT.DJANGO.SEC.039
  name: Set Password
  ecosystem: django
  category: security
  description: '''$VAR'' is the empty string and is being used to set the password
    on ''$MODEL''. If you meant to set an unusable password, set the password to None
    or call ''set_unusable_password()''.'
  patterns:
  - $MODEL.set_password($EMPTY)
  - $MODEL.set_password($X)
  source_rules:
  - unvalidated-password
  - password-empty-string
  tier: T2
- id: EXT.DJANGO.SEC.040
  name: W Get
  ecosystem: django
  category: security
  description: '''$VAR'' is using the empty string as its default and is being used
    to set the password on ''$MODEL''. If you meant to set an unusable password, set
    the default value to ''None'' or call ''set_unusable_passw'
  patterns:
  - requests.$METHOD(..., f"...{request.$W.get(...)}...", ...)
  - $V = request.$W.get($X)
  - os.system(..., $S % request.$W.get(...), ...)
  - $MODEL.objects.extra(..., where=[..., f"...{request.$W.get(...)}...", ...], ...)
  - django.shortcuts.redirect(..., request.$W.get(...), ...)
  source_rules:
  - user-eval
  - user-eval-format-string
  - path-traversal-open
  - path-traversal-join
  - sql-injection-using-extra-where
  - use-none-for-password-default
  - reflected-data-httpresponse
  - path-traversal-file-name
  - reflected-data-httpresponsebadrequest
  - open-redirect
  tier: T2
- id: EXT.DJANGO.SEC.041
  name: Utils Safestring Mark Safe
  ecosystem: django
  category: security
  description: '''mark_safe()'' is used to mark a string as "safe" for HTML output.
    This disables escaping and could therefore subject the content to XSS attacks.
    Use ''django.utils.html.format_html()'' to build HTML for'
  patterns:
  - django.utils.safestring.mark_safe(...)
  source_rules:
  - avoid-mark-safe
  tier: T2
- id: EXT.DJANGO.SEC.042
  name: Mark Safe
  ecosystem: django
  category: security
  description: '''mark_safe()'' is used to mark a string as "safe" for HTML output.
    This disables escaping and could therefore subject the content to XSS attacks.
    Use ''django.utils.html.format_html()'' to build HTML for'
  patterns:
  - django.utils.safestring.mark_safe(...)
  source_rules:
  - avoid-mark-safe
  tier: T2
- id: EXT.DJANGO.SEC.043
  name: Expression As Sql
  ecosystem: django
  category: security
  description: 'Detected a Custom Expression ''''$EXPRESSION'''' calling ''''as_sql(...).''''
    This could lead to SQL injection, which can result in attackers exfiltrating sensitive
    data. Instead, ensure no user input enters '
  patterns:
  - $EXPRESSION.as_sql(...)
  source_rules:
  - custom-expression-as-sql
  tier: T2
- id: EXT.DJANGO.SEC.044
  name: As Sql
  ecosystem: django
  category: security
  description: 'Detected a Custom Expression ''''$EXPRESSION'''' calling ''''as_sql(...).''''
    This could lead to SQL injection, which can result in attackers exfiltrating sensitive
    data. Instead, ensure no user input enters '
  patterns:
  - $EXPRESSION.as_sql(...)
  source_rules:
  - custom-expression-as-sql
  tier: T2
- id: EXT.DJANGO.SEC.045
  name: Db Models Func
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Func, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.046
  name: ', Django Db Models Func, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Func, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.047
  name: Db Models Expressions Func
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Func, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.048
  name: ', Django Db Models Expressions Func, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Func, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.049
  name: Db Models Expression
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Expression, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.050
  name: ', Django Db Models Expression, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Expression, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.051
  name: Db Models Expressions Expression
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Expression, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.052
  name: ', Django Db Models Expressions Expression, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Expression, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.053
  name: Db Models Value
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Value, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.054
  name: ', Django Db Models Value, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Value, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.055
  name: Db Models Expressions Value
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Value, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.056
  name: ', Django Db Models Expressions Value, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Value, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.057
  name: Db Models Durationvalue
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.DurationValue, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.058
  name: ', Django Db Models Durationvalue, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.DurationValue, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.059
  name: Db Models Expressions Durationvalue
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.DurationValue, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.060
  name: ', Django Db Models Expressions Durationvalue, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.DurationValue, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.061
  name: Db Models Rawsql
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.RawSQL, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.062
  name: ', Django Db Models Rawsql, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.RawSQL, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.063
  name: Db Models Expressions Rawsql
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - django.db.models.expressions.RawSQL(..., $S.format(..., request.$W.get(...), ...),
    ...)
  - django.db.models.expressions.RawSQL(..., f"...{request.$W[...]}...", ...)
  - return django.db.models.expressions.RawSQL(..., request.$W(...), ...)
  - django.db.models.expressions.RawSQL(...)
  - $A = django.db.models.expressions.RawSQL(..., request.$W.get(...), ...)
  source_rules:
  - sql-injection-using-rawsql
  - avoid-raw-sql
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.064
  name: ', Django Db Models Expressions Rawsql, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.RawSQL, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.065
  name: Db Models Star
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Star, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.066
  name: ', Django Db Models Star, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Star, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.067
  name: Db Models Expressions Star
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Star, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.068
  name: ', Django Db Models Expressions Star, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Star, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.069
  name: Db Models Random
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Random, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.070
  name: ', Django Db Models Random, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Random, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.071
  name: Db Models Expressions Random
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Random, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.072
  name: ', Django Db Models Expressions Random, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Random, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.073
  name: Db Models Col
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Col, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.074
  name: ', Django Db Models Col, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Col, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.075
  name: Db Models Expressions Col
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Col, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.076
  name: ', Django Db Models Expressions Col, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Col, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.077
  name: Db Models Ref
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Ref, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.078
  name: ', Django Db Models Ref, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Ref, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.079
  name: Db Models Expressions Ref
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Ref, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.080
  name: ', Django Db Models Expressions Ref, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Ref, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.081
  name: Db Models Expressionlist
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.ExpressionList, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.082
  name: ', Django Db Models Expressionlist, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.ExpressionList, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.083
  name: Db Models Expressions Expressionlist
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.ExpressionList, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.084
  name: ', Django Db Models Expressions Expressionlist, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.ExpressionList, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.085
  name: Db Models Expressionwrapper
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.ExpressionWrapper, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.086
  name: ', Django Db Models Expressionwrapper, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.ExpressionWrapper, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.087
  name: Db Models Expressions Expressionwrapper
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.ExpressionWrapper, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.088
  name: ', Django Db Models Expressions Expressionwrapper, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.ExpressionWrapper, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.089
  name: Db Models When
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.When, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.090
  name: ', Django Db Models When, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.When, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.091
  name: Db Models Expressions When
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.When, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.092
  name: ', Django Db Models Expressions When, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.When, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.093
  name: Db Models Case
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Case, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.094
  name: ', Django Db Models Case, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Case, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.095
  name: Db Models Expressions Case
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Case, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.096
  name: ', Django Db Models Expressions Case, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Case, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.097
  name: Db Models Subquery
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Subquery, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.098
  name: ', Django Db Models Subquery, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Subquery, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.099
  name: Db Models Expressions Subquery
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Subquery, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.100
  name: ', Django Db Models Expressions Subquery, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Subquery, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.101
  name: Db Models Exists
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Exists, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.102
  name: ', Django Db Models Exists, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Exists, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.103
  name: Db Models Expressions Exists
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Exists, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.104
  name: ', Django Db Models Expressions Exists, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Exists, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.105
  name: Db Models Window
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Window, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.106
  name: ', Django Db Models Window, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.Window, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.107
  name: Db Models Expressions Window
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Window, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.108
  name: ', Django Db Models Expressions Window, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.Window, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.109
  name: Db Models Windowframe
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.WindowFrame, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.110
  name: ', Django Db Models Windowframe, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.WindowFrame, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.111
  name: Db Models Expressions Windowframe
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.WindowFrame, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.112
  name: ', Django Db Models Expressions Windowframe, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.WindowFrame, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.113
  name: Db Models Rowrange
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.RowRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.114
  name: ', Django Db Models Rowrange, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.RowRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.115
  name: Db Models Expressions Rowrange
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.RowRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.116
  name: ', Django Db Models Expressions Rowrange, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.RowRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.117
  name: Db Models Valuerange
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.ValueRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.118
  name: ', Django Db Models Valuerange, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.ValueRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.119
  name: Db Models Expressions Valuerange
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.ValueRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.120
  name: ', Django Db Models Expressions Valuerange, '
  ecosystem: django
  category: security
  description: 'Found extension of custom expression: $CLASS. Extending expressions
    in this way could inadvertently lead to a SQL injection vulnerability, which can
    result in attackers exfiltrating sensitive data. In'
  patterns:
  - 'class $CLASS(..., django.db.models.expressions.ValueRange, ...):'
  source_rules:
  - extends-custom-expression
  tier: T2
- id: EXT.DJANGO.SEC.121
  name: Response Set Cookie
  ecosystem: django
  category: security
  description: 'Django cookies should be handled securely by setting secure=True,
    httponly=True, and samesite=''Lax'' in response.set_cookie(...). If your situation
    calls for different settings, explicitly disable the '
  patterns:
  - response.set_cookie(...)
  source_rules:
  - django-secure-set-cookie
  tier: T2
- id: EXT.DJANGO.SEC.122
  name: Set Cookie
  ecosystem: django
  category: security
  description: 'Django cookies should be handled securely by setting secure=True,
    httponly=True, and samesite=''Lax'' in response.set_cookie(...). If your situation
    calls for different settings, explicitly disable the '
  patterns:
  - response.set_cookie(...)
  source_rules:
  - django-secure-set-cookie
  tier: T2
- id: EXT.DJANGO.SEC.123
  name: Views Decorators Csrf Csrf Exempt
  ecosystem: django
  category: security
  description: Detected usage of @csrf_exempt, which indicates that there is no CSRF
    token set for this route. This could lead to an attacker manipulating the user's
    account and exfiltration of private data. Instead
  patterns:
  - '@django.views.decorators.csrf.csrf_exempt'
  source_rules:
  - no-csrf-exempt
  tier: T2
- id: EXT.DJANGO.SEC.124
  name: '@Django Views Decorators Csrf Csrf Exempt'
  ecosystem: django
  category: security
  description: Detected usage of @csrf_exempt, which indicates that there is no CSRF
    token set for this route. This could lead to an attacker manipulating the user's
    account and exfiltration of private data. Instead
  patterns:
  - '@django.views.decorators.csrf.csrf_exempt'
  source_rules:
  - no-csrf-exempt
  tier: T2
- id: EXT.DJANGO.SEC.125
  name: Model Extra
  ecosystem: django
  category: security
  description: QuerySet.extra' does not provide safeguards against SQL injection and
    requires very careful use. SQL injection can lead to critical data being stolen
    by attackers. Instead of using '.extra', use the D
  patterns:
  - $MODEL.extra(...)
  source_rules:
  - avoid-query-set-extra
  tier: T2
- id: EXT.DJANGO.SEC.126
  name: Extra
  ecosystem: django
  category: security
  description: QuerySet.extra' does not provide safeguards against SQL injection and
    requires very careful use. SQL injection can lead to critical data being stolen
    by attackers. Instead of using '.extra', use the D
  patterns:
  - $MODEL.objects.extra(..., where=[..., f"...{request.$W.get(...)}...", ...], ...)
  - return $MODEL.objects.extra(..., where=[..., request.$W[...], ...], ...)
  - return $MODEL.objects.extra(..., where=[..., request.$W, ...], ...)
  - $MODEL.objects.extra(..., where=[..., $S.format(..., request.$W[...], ...), ...],
    ...)
  - $A = $MODEL.objects.extra(..., where=[..., request.$W(...), ...], ...)
  source_rules:
  - avoid-query-set-extra
  - sql-injection-using-extra-where
  tier: T2
- id: EXT.DJANGO.SEC.127
  name: Model Objects Raw
  ecosystem: django
  category: security
  description: Detected the use of 'RawSQL' or 'raw' indicating the execution of a
    non-parameterized SQL query. This could lead to a SQL injection and therefore
    protected information could be leaked. Instead, use Dj
  patterns:
  - $MODEL.objects.raw($QUERY, ...)
  - $MODEL.objects.raw(..., $S % request.$W.get(...), ...)
  - $MODEL.objects.raw(..., request.$W, ...)
  - return $MODEL.objects.raw(..., request.$W(...), ...)
  - $A = $MODEL.objects.raw(..., request.$W[...], ...)
  source_rules:
  - avoid-raw-sql
  - sql-injection-using-raw
  tier: T2
- id: EXT.DJANGO.SEC.128
  name: Raw
  ecosystem: django
  category: security
  description: Detected the use of 'RawSQL' or 'raw' indicating the execution of a
    non-parameterized SQL query. This could lead to a SQL injection and therefore
    protected information could be leaked. Instead, use Dj
  patterns:
  - $MODEL.objects.raw($QUERY, ...)
  - $MODEL.objects.raw(..., $S % request.$W.get(...), ...)
  - $MODEL.objects.raw(..., request.$W, ...)
  - return $MODEL.objects.raw(..., request.$W(...), ...)
  - $A = $MODEL.objects.raw(..., request.$W[...], ...)
  source_rules:
  - avoid-raw-sql
  - sql-injection-using-raw
  tier: T2
- id: EXT.DJANGO.SEC.129
  name: Rawsql
  ecosystem: django
  category: security
  description: Detected the use of 'RawSQL' or 'raw' indicating the execution of a
    non-parameterized SQL query. This could lead to a SQL injection and therefore
    protected information could be leaked. Instead, use Dj
  patterns:
  - django.db.models.expressions.RawSQL(..., $S.format(..., request.$W.get(...), ...),
    ...)
  - django.db.models.expressions.RawSQL(..., f"...{request.$W[...]}...", ...)
  - return django.db.models.expressions.RawSQL(..., request.$W(...), ...)
  - django.db.models.expressions.RawSQL(...)
  - $A = django.db.models.expressions.RawSQL(..., request.$W.get(...), ...)
  source_rules:
  - sql-injection-using-rawsql
  - avoid-raw-sql
  tier: T2
- id: EXT.DJANGO.SEC.130
  name: Reqfunc Get
  ecosystem: django
  category: security
  description: Avoid using insecure deserialization library, backed by `pickle`, `_pickle`,
    `cpickle`, `dill`, `shelve`, or `yaml`, which are known to lead to remote code
    execution vulnerabilities.
  patterns:
  - request.$REQFUNC.get(...)
  source_rules:
  - avoid-insecure-deserialization
  tier: T2
- id: EXT.DJANGO.SEC.131
  name: Shortcuts Redirect
  ecosystem: django
  category: security
  description: Data from request ($DATA) is passed to redirect(). This is an open
    redirect and could be exploited. Ensure you are redirecting to safe URLs by using
    django.utils.http.is_safe_url(). See https://cwe.mi
  patterns:
  - django.shortcuts.redirect(..., $S.format(..., request.$W(...), ...), ...)
  - django.shortcuts.redirect(..., request.$W[...], ...)
  - return django.shortcuts.redirect(..., $S.format(..., request.$W[...], ...), ...)
  - django.shortcuts.redirect(..., $S.format(..., request.$W.get(...), ...), ...)
  - django.shortcuts.redirect(..., $S % request.$W.get(...), ...)
  source_rules:
  - open-redirect
  tier: T2
- id: EXT.DJANGO.SEC.132
  name: Redirect
  ecosystem: django
  category: security
  description: Data from request ($DATA) is passed to redirect(). This is an open
    redirect and could be exploited. Ensure you are redirecting to safe URLs by using
    django.utils.http.is_safe_url(). See https://cwe.mi
  patterns:
  - django.shortcuts.redirect(..., $S.format(..., request.$W(...), ...), ...)
  - django.shortcuts.redirect(..., request.$W[...], ...)
  - return django.shortcuts.redirect(..., $S.format(..., request.$W[...], ...), ...)
  - django.shortcuts.redirect(..., $S.format(..., request.$W.get(...), ...), ...)
  - django.shortcuts.redirect(..., $S % request.$W.get(...), ...)
  source_rules:
  - open-redirect
  tier: T2
- id: EXT.DJANGO.SEC.133
  name: S Format
  ecosystem: django
  category: security
  description: Data from request ($DATA) is passed to redirect(). This is an open
    redirect and could be exploited. Ensure you are redirecting to safe URLs by using
    django.utils.http.is_safe_url(). See https://cwe.mi
  patterns:
  - django.shortcuts.redirect(..., $S.format(..., request.$W(...), ...), ...)
  - $A = os.system(..., $S.format(..., request.$W, ...), ...)
  - $A = django.shortcuts.redirect(..., $S.format(..., request.$W, ...), ...)
  - $A = django.shortcuts.redirect(..., $S.format(..., request.$W[...], ...), ...)
  - $A = django.http.HttpResponseRedirect(..., $S.format(..., request.$W, ...), ...)
  source_rules:
  - ssrf-injection-urllib
  - sql-injection-db-cursor-execute
  - path-traversal-open
  - sql-injection-using-raw
  - sql-injection-using-rawsql
  - reflected-data-httpresponsebadrequest
  - command-injection-os-system
  - open-redirect
  - sql-injection-using-extra-where
  - ssrf-injection-requests
  tier: T2
- id: EXT.DJANGO.SEC.134
  name: Format
  ecosystem: django
  category: security
  description: Data from request ($DATA) is passed to redirect(). This is an open
    redirect and could be exploited. Ensure you are redirecting to safe URLs by using
    django.utils.http.is_safe_url(). See https://cwe.mi
  patterns:
  - django.shortcuts.redirect(..., $S.format(..., request.$W(...), ...), ...)
  - $A = os.system(..., $S.format(..., request.$W, ...), ...)
  - $A = django.shortcuts.redirect(..., $S.format(..., request.$W, ...), ...)
  - $A = django.shortcuts.redirect(..., $S.format(..., request.$W[...], ...), ...)
  - exec(..., $STR.format(..., request.$W[...], ...), ...)
  source_rules:
  - ssrf-injection-urllib
  - sql-injection-db-cursor-execute
  - user-eval-format-string
  - path-traversal-open
  - sql-injection-using-raw
  - sql-injection-using-rawsql
  - reflected-data-httpresponsebadrequest
  - tainted-sql-string
  - command-injection-os-system
  - open-redirect
  tier: T2
- id: EXT.DJANGO.SEC.135
  name: Http Httpresponseredirect
  ecosystem: django
  category: security
  description: Data from request ($DATA) is passed to redirect(). This is an open
    redirect and could be exploited. Ensure you are redirecting to safe URLs by using
    django.utils.http.is_safe_url(). See https://cwe.mi
  patterns:
  - $A = django.http.HttpResponseRedirect(..., $S.format(..., request.$W.get(...),
    ...), ...)
  - return django.http.HttpResponseRedirect(..., $S % request.$W, ...)
  - $A = django.http.HttpResponseRedirect(..., request.$W(...), ...)
  - return django.http.HttpResponseRedirect(..., request.$W(...), ...)
  - $A = django.http.HttpResponseRedirect(..., f"...{request.$W(...)}...", ...)
  source_rules:
  - open-redirect
  tier: T2
- id: EXT.DJANGO.SEC.136
  name: Httpresponseredirect
  ecosystem: django
  category: security
  description: Data from request ($DATA) is passed to redirect(). This is an open
    redirect and could be exploited. Ensure you are redirecting to safe URLs by using
    django.utils.http.is_safe_url(). See https://cwe.mi
  patterns:
  - $A = django.http.HttpResponseRedirect(..., $S.format(..., request.$W.get(...),
    ...), ...)
  - return django.http.HttpResponseRedirect(..., $S % request.$W, ...)
  - $A = django.http.HttpResponseRedirect(..., request.$W(...), ...)
  - return django.http.HttpResponseRedirect(..., request.$W(...), ...)
  - $A = django.http.HttpResponseRedirect(..., f"...{request.$W(...)}...", ...)
  source_rules:
  - open-redirect
  tier: T2
- id: EXT.DJANGO.SEC.137
  name: Http Httpresponse
  ecosystem: django
  category: security
  description: Found user-controlled request data passed into HttpResponse. This could
    be vulnerable to XSS, leading to attackers gaining access to user cookies and
    protected information. Ensure that the request dat
  patterns:
  - django.http.HttpResponse(..., f"...{request.$W}...", ...)
  - django.http.HttpResponse(..., $S % request.$W(...), ...)
  - django.http.HttpResponse(...,content_type=$TYPE,...)
  - return django.http.HttpResponse(..., request.$W.get(...), ...)
  - django.http.HttpResponse(..., $S % request.$W[...], ...)
  source_rules:
  - reflected-data-httpresponse
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.138
  name: Httpresponse
  ecosystem: django
  category: security
  description: Found user-controlled request data passed into HttpResponse. This could
    be vulnerable to XSS, leading to attackers gaining access to user cookies and
    protected information. Ensure that the request dat
  patterns:
  - django.http.HttpResponse(..., f"...{request.$W}...", ...)
  - django.http.HttpResponse(..., $S % request.$W(...), ...)
  - django.http.HttpResponse(...,content_type=$TYPE,...)
  - return django.http.HttpResponse(..., request.$W.get(...), ...)
  - django.http.HttpResponse(..., $S % request.$W[...], ...)
  source_rules:
  - reflected-data-httpresponse
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.139
  name: Model Objects Create
  ecosystem: django
  category: security
  description: Mass assignment detected. This can result in assignment to model fields
    that are unintended and can be exploited by an attacker. Instead of using '**request.$W',
    assign each field you want to edit ind
  patterns:
  - $MODEL.objects.create(**request.$W)
  source_rules:
  - mass-assignment
  tier: T2
- id: EXT.DJANGO.SEC.140
  name: Create
  ecosystem: django
  category: security
  description: Mass assignment detected. This can result in assignment to model fields
    that are unintended and can be exploited by an attacker. Instead of using '**request.$W',
    assign each field you want to edit ind
  patterns:
  - $MODEL.objects.create(**request.$W)
  source_rules:
  - mass-assignment
  tier: T2
- id: EXT.DJANGO.SEC.141
  name: Obj Update
  ecosystem: django
  category: security
  description: Mass assignment detected. This can result in assignment to model fields
    that are unintended and can be exploited by an attacker. Instead of using '**request.$W',
    assign each field you want to edit ind
  patterns:
  - $OBJ.update(**request.$W)
  source_rules:
  - mass-assignment
  tier: T2
- id: EXT.DJANGO.SEC.142
  name: Update
  ecosystem: django
  category: security
  description: Mass assignment detected. This can result in assignment to model fields
    that are unintended and can be exploited by an attacker. Instead of using '**request.$W',
    assign each field you want to edit ind
  patterns:
  - $OBJ.update(**request.$W)
  source_rules:
  - mass-assignment
  tier: T2
- id: EXT.DJANGO.SEC.143
  name: Http Httpresponsebadrequest
  ecosystem: django
  category: security
  description: Found user-controlled request data passed into a HttpResponseBadRequest.
    This could be vulnerable to XSS, leading to attackers gaining access to user cookies
    and protected information. Ensure that the
  patterns:
  - return django.http.HttpResponseBadRequest(..., request.$W, ...)
  - django.http.HttpResponseBadRequest(..., $S.format(..., request.$W(...), ...),
    ...)
  - django.http.HttpResponseBadRequest(..., $S % request.$W, ...)
  - django.http.HttpResponseBadRequest(..., $S % request.$W(...), ...)
  - $A = django.http.HttpResponseBadRequest(..., request.$W[...], ...)
  source_rules:
  - reflected-data-httpresponsebadrequest
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.144
  name: Httpresponsebadrequest
  ecosystem: django
  category: security
  description: Found user-controlled request data passed into a HttpResponseBadRequest.
    This could be vulnerable to XSS, leading to attackers gaining access to user cookies
    and protected information. Ensure that the
  patterns:
  - return django.http.HttpResponseBadRequest(..., request.$W, ...)
  - django.http.HttpResponseBadRequest(..., $S.format(..., request.$W(...), ...),
    ...)
  - django.http.HttpResponseBadRequest(..., $S % request.$W, ...)
  - django.http.HttpResponseBadRequest(..., $S % request.$W(...), ...)
  - $A = django.http.HttpResponseBadRequest(..., request.$W[...], ...)
  source_rules:
  - reflected-data-httpresponsebadrequest
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.145
  name: Http Fileresponse
  ecosystem: django
  category: security
  description: Found user-controlled request data being passed into a file open, which
    is them passed as an argument into the FileResponse. This is dangerous because
    an attacker could specify an arbitrary file to re
  patterns:
  - django.http.FileResponse(..., request.$W(...), ...)
  - django.http.FileResponse(..., request.$W[...], ...)
  - return django.http.FileResponse(..., request.$W, ...)
  - django.http.FileResponse(..., request.$W, ...)
  - return django.http.FileResponse(..., request.$W[...], ...)
  source_rules:
  - request-data-fileresponse
  tier: T2
- id: EXT.DJANGO.SEC.146
  name: Fileresponse
  ecosystem: django
  category: security
  description: Found user-controlled request data being passed into a file open, which
    is them passed as an argument into the FileResponse. This is dangerous because
    an attacker could specify an arbitrary file to re
  patterns:
  - django.http.FileResponse(..., request.$W(...), ...)
  - django.http.FileResponse(..., request.$W[...], ...)
  - return django.http.FileResponse(..., request.$W, ...)
  - django.http.FileResponse(..., request.$W, ...)
  - return django.http.FileResponse(..., request.$W[...], ...)
  source_rules:
  - request-data-fileresponse
  tier: T2
- id: EXT.DJANGO.SEC.147
  name: Utils Html Escape
  ecosystem: django
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - django.utils.html.escape(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.DJANGO.SEC.148
  name: Escape
  ecosystem: django
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - django.utils.html.escape(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.DJANGO.SEC.149
  name: Html Format
  ecosystem: django
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - $HTML.format(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.DJANGO.SEC.150
  name: F Write
  ecosystem: django
  category: security
  description: Found user-controlled request data passed into '.write(...)'. This
    could be dangerous if a malicious actor is able to control data into sensitive
    files. For example, a malicious actor could force roll
  patterns:
  - return $F.write(..., request.$W.get(...), ...)
  - $A = $F.write(..., request.$W.get(...), ...)
  - $F.write(..., request.$W.get(...), ...)
  - $F.write(..., request.$W(...), ...)
  - return $F.write(..., request.$W, ...)
  source_rules:
  - request-data-write
  tier: T2
- id: EXT.DJANGO.SEC.151
  name: Write
  ecosystem: django
  category: security
  description: Found user-controlled request data passed into '.write(...)'. This
    could be dangerous if a malicious actor is able to control data into sensitive
    files. For example, a malicious actor could force roll
  patterns:
  - return $F.write(..., request.$W.get(...), ...)
  - $A = $F.write(..., request.$W.get(...), ...)
  - $F.write(..., request.$W.get(...), ...)
  - $F.write(..., request.$W(...), ...)
  - return $F.write(..., request.$W, ...)
  source_rules:
  - request-data-write
  tier: T2
- id: EXT.DJANGO.SEC.152
  name: Urllib Request Urlopen
  ecosystem: django
  category: security
  description: Data from request object is passed to a new server-side request. This
    could lead to a server-side request forgery (SSRF), which could result in attackers
    gaining access to private organization data. T
  patterns:
  - urllib.request.urlopen(..., $S.format(..., request.$W(...), ...), ...)
  - urllib.request.urlopen(..., request.$W, ...)
  - return urllib.request.urlopen(..., request.$W, ...)
  - $A = urllib.request.urlopen(..., request.$W, ...)
  - urllib.request.urlopen(..., f"...{request.$W(...)}...", ...)
  source_rules:
  - ssrf-injection-urllib
  tier: T2
- id: EXT.DJANGO.SEC.153
  name: Urlopen
  ecosystem: django
  category: security
  description: Data from request object is passed to a new server-side request. This
    could lead to a server-side request forgery (SSRF), which could result in attackers
    gaining access to private organization data. T
  patterns:
  - urllib.request.urlopen(..., $S.format(..., request.$W(...), ...), ...)
  - urllib.request.urlopen(..., request.$W, ...)
  - return urllib.request.urlopen(..., request.$W, ...)
  - $A = urllib.request.urlopen(..., request.$W, ...)
  - urllib.request.urlopen(..., f"...{request.$W(...)}...", ...)
  source_rules:
  - ssrf-injection-urllib
  tier: T2
- id: EXT.DJANGO.SEC.154
  name: Str Format
  ecosystem: django
  category: security
  description: Found user data in a call to 'exec'. This is extremely dangerous because
    it can enable an attacker to execute arbitrary remote code on the system. Instead,
    refactor your code to not use 'eval' and ins
  patterns:
  - eval(..., $STR.format(..., request.$W(...), ...), ...)
  - eval(..., $STR.format(..., request.$W[...], ...), ...)
  - exec(..., $STR.format(..., request.$W[...], ...), ...)
  - exec(..., $STR.format(..., request.$W.get(...), ...), ...)
  - exec(..., $STR.format(..., request.$W(...), ...), ...)
  source_rules:
  - user-exec-format-string
  - user-eval-format-string
  tier: T2
- id: EXT.DJANGO.SEC.155
  name: Base64 Decodestring
  ecosystem: django
  category: security
  description: Found user data in a call to 'exec'. This is extremely dangerous because
    it can enable an attacker to execute arbitrary remote code on the system. Instead,
    refactor your code to not use 'eval' and ins
  patterns:
  - exec(..., base64.decodestring(bytes(f"...{request.$W.get(...)}...", ...), ...),
    ...)
  - exec(..., base64.decodestring(f"...{request.$W.get(...)}...", ...), ...)
  - exec(..., base64.decodestring(bytes(request.$W.get(...), ...), ...), ...)
  - exec(..., base64.decodestring(bytes($S % request.$W.get(...), ...), ...), ...)
  - exec(..., base64.decodestring(bytes($S.format(..., request.$W.get(...), ...),
    ...), ...), ...)
  source_rules:
  - user-exec-format-string
  tier: T2
- id: EXT.DJANGO.SEC.156
  name: Decodestring
  ecosystem: django
  category: security
  description: Found user data in a call to 'exec'. This is extremely dangerous because
    it can enable an attacker to execute arbitrary remote code on the system. Instead,
    refactor your code to not use 'eval' and ins
  patterns:
  - exec(..., base64.decodestring(bytes(f"...{request.$W.get(...)}...", ...), ...),
    ...)
  - exec(..., base64.decodestring(f"...{request.$W.get(...)}...", ...), ...)
  - exec(..., base64.decodestring(bytes(request.$W.get(...), ...), ...), ...)
  - exec(..., base64.decodestring(bytes($S % request.$W.get(...), ...), ...), ...)
  - exec(..., base64.decodestring(bytes($S.format(..., request.$W.get(...), ...),
    ...), ...), ...)
  source_rules:
  - user-exec-format-string
  tier: T2
- id: EXT.DJANGO.SEC.157
  name: Asyncio Get Running Loop
  ecosystem: django
  category: security
  description: Found user data in a call to 'exec'. This is extremely dangerous because
    it can enable an attacker to execute arbitrary remote code on the system. Instead,
    refactor your code to not use 'eval' and ins
  patterns:
  - loop = asyncio.get_running_loop()
  source_rules:
  - user-exec
  tier: T2
- id: EXT.DJANGO.SEC.158
  name: Get Running Loop
  ecosystem: django
  category: security
  description: Found user data in a call to 'exec'. This is extremely dangerous because
    it can enable an attacker to execute arbitrary remote code on the system. Instead,
    refactor your code to not use 'eval' and ins
  patterns:
  - loop = asyncio.get_running_loop()
  source_rules:
  - user-exec
  tier: T2
- id: EXT.DJANGO.SEC.159
  name: Os Path Join
  ecosystem: django
  category: security
  description: Data from request is passed to os.path.join() and to open(). This is
    a path traversal vulnerability, which can lead to sensitive data being leaked.
    To mitigate, consider using os.path.abspath or os.pa
  patterns:
  - open(os.path.join(..., request.$W[...], ...), ...)
  - open(os.path.join(..., request.$W, ...), ...)
  - $P = os.path.join(..., request.$W.get(...), ...)
  - $P = os.path.join(..., request.$W(...), ...)
  - $P = os.path.join(..., request.$W[...], ...)
  source_rules:
  - path-traversal-join
  tier: T2
- id: EXT.DJANGO.SEC.160
  name: Join
  ecosystem: django
  category: security
  description: Data from request is passed to os.path.join() and to open(). This is
    a path traversal vulnerability, which can lead to sensitive data being leaked.
    To mitigate, consider using os.path.abspath or os.pa
  patterns:
  - open(os.path.join(..., request.$W[...], ...), ...)
  - open(os.path.join(..., request.$W, ...), ...)
  - $P = os.path.join(..., request.$W.get(...), ...)
  - $P = os.path.join(..., request.$W(...), ...)
  - $P = os.path.join(..., request.$W[...], ...)
  source_rules:
  - path-traversal-join
  tier: T2
- id: EXT.DJANGO.SEC.161
  name: Os System
  ecosystem: django
  category: security
  description: Request data detected in os.system. This could be vulnerable to a command
    injection and should be avoided. If this must be done, use the 'subprocess' module
    instead and pass the arguments as a list. S
  patterns:
  - os.system(..., $S % request.$W(...), ...)
  - $A = os.system(..., request.$W[...], ...)
  - return os.system(..., f"...{request.$W(...)}...", ...)
  - return os.system(..., request.$W, ...)
  - os.system(..., f"...{request.$W(...)}...", ...)
  source_rules:
  - command-injection-os-system
  tier: T2
- id: EXT.DJANGO.SEC.162
  name: System
  ecosystem: django
  category: security
  description: Request data detected in os.system. This could be vulnerable to a command
    injection and should be avoided. If this must be done, use the 'subprocess' module
    instead and pass the arguments as a list. S
  patterns:
  - os.system(..., $S % request.$W(...), ...)
  - $A = os.system(..., request.$W[...], ...)
  - return os.system(..., f"...{request.$W(...)}...", ...)
  - return os.system(..., request.$W, ...)
  - os.system(..., f"...{request.$W(...)}...", ...)
  source_rules:
  - command-injection-os-system
  tier: T2
- id: EXT.DJANGO.SEC.163
  name: Core Mail Send Mail
  ecosystem: django
  category: security
  description: Found request data in 'send_mail(...)' that uses 'html_message'. This
    is dangerous because HTML emails are susceptible to XSS. An attacker could inject
    data into this HTML email, causing XSS.
  patterns:
  - return django.core.mail.send_mail(..., html_message=request.$W[...], ...)
  - $A = django.core.mail.send_mail(..., html_message=request.$W.get(...), ...)
  - django.core.mail.send_mail(..., html_message=request.$W[...], ...)
  - return django.core.mail.send_mail(..., html_message=request.$W.get(...), ...)
  - return django.core.mail.send_mail(..., html_message=request.$W, ...)
  source_rules:
  - xss-send-mail-html-message
  tier: T2
- id: EXT.DJANGO.SEC.164
  name: Send Mail
  ecosystem: django
  category: security
  description: Found request data in 'send_mail(...)' that uses 'html_message'. This
    is dangerous because HTML emails are susceptible to XSS. An attacker could inject
    data into this HTML email, causing XSS.
  patterns:
  - return django.core.mail.send_mail(..., html_message=request.$W[...], ...)
  - $A = django.core.mail.send_mail(..., html_message=request.$W.get(...), ...)
  - django.core.mail.send_mail(..., html_message=request.$W[...], ...)
  - return django.core.mail.send_mail(..., html_message=request.$W.get(...), ...)
  - return django.core.mail.send_mail(..., html_message=request.$W, ...)
  source_rules:
  - xss-send-mail-html-message
  tier: T2
- id: EXT.DJANGO.SEC.165
  name: Core Mail Emailmessage
  ecosystem: django
  category: security
  description: Found request data in an EmailMessage that is set to use HTML. This
    is dangerous because HTML emails are susceptible to XSS. An attacker could inject
    data into this HTML email, causing XSS.
  patterns:
  - django.core.mail.EmailMessage($SUBJ, request.$W[...], ...)
  - django.core.mail.EmailMessage($SUBJ, request.$W.get(...), ...)
  - django.core.mail.EmailMessage($SUBJ, request.$W, ...)
  - return django.core.mail.EmailMessage($SUBJ, request.$W[...], ...)
  - return django.core.mail.EmailMessage($SUBJ, request.$W, ...)
  source_rules:
  - xss-html-email-body
  tier: T2
- id: EXT.DJANGO.SEC.166
  name: Emailmessage
  ecosystem: django
  category: security
  description: Found request data in an EmailMessage that is set to use HTML. This
    is dangerous because HTML emails are susceptible to XSS. An attacker could inject
    data into this HTML email, causing XSS.
  patterns:
  - django.core.mail.EmailMessage($SUBJ, request.$W[...], ...)
  - django.core.mail.EmailMessage($SUBJ, request.$W.get(...), ...)
  - django.core.mail.EmailMessage($SUBJ, request.$W, ...)
  - return django.core.mail.EmailMessage($SUBJ, request.$W[...], ...)
  - return django.core.mail.EmailMessage($SUBJ, request.$W, ...)
  source_rules:
  - xss-html-email-body
  tier: T2
- id: EXT.DJANGO.SEC.167
  name: Model Objects Extra
  ecosystem: django
  category: security
  description: User-controlled data from a request is passed to 'extra()'. This could
    lead to a SQL injection and therefore protected information could be leaked. Instead,
    use parameterized queries or escape the use
  patterns:
  - $MODEL.objects.extra(..., where=[..., f"...{request.$W.get(...)}...", ...], ...)
  - return $MODEL.objects.extra(..., where=[..., request.$W[...], ...], ...)
  - return $MODEL.objects.extra(..., where=[..., request.$W, ...], ...)
  - $MODEL.objects.extra(..., where=[..., $S.format(..., request.$W[...], ...), ...],
    ...)
  - $A = $MODEL.objects.extra(..., where=[..., request.$W(...), ...], ...)
  source_rules:
  - sql-injection-using-extra-where
  tier: T2
- id: EXT.DJANGO.SEC.168
  name: Cursor Execute
  ecosystem: django
  category: security
  description: User-controlled data from a request is passed to 'execute()'. This
    could lead to a SQL injection and therefore protected information could be leaked.
    Instead, use django's QuerySets, which are built w
  patterns:
  - $CURSOR.execute(..., $S % request.$W.get(...), ...)
  - $CURSOR.execute(..., $S.format(..., request.$W[...], ...), ...)
  - return $CURSOR.execute(..., request.$W.get(...), ...)
  - $CURSOR.execute(..., $S % request.$W(...), ...)
  - $A = $CURSOR.execute(..., request.$W[...], ...)
  source_rules:
  - sql-injection-db-cursor-execute
  tier: T2
- id: EXT.DJANGO.SEC.169
  name: Execute
  ecosystem: django
  category: security
  description: User-controlled data from a request is passed to 'execute()'. This
    could lead to a SQL injection and therefore protected information could be leaked.
    Instead, use django's QuerySets, which are built w
  patterns:
  - $CURSOR.execute(..., $S % request.$W.get(...), ...)
  - $CURSOR.execute(..., $S.format(..., request.$W[...], ...), ...)
  - return $CURSOR.execute(..., request.$W.get(...), ...)
  - $CURSOR.execute(..., $S % request.$W(...), ...)
  - $A = $CURSOR.execute(..., request.$W[...], ...)
  source_rules:
  - sql-injection-db-cursor-execute
  tier: T2
- id: EXT.DJANGO.SEC.170
  name: Http Httpresponsenotfound
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseNotFound(...)
  - django.http.HttpResponseNotFound(...,content_type=$TYPE,...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.171
  name: Httpresponsenotfound
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseNotFound(...)
  - django.http.HttpResponseNotFound(...,content_type=$TYPE,...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.172
  name: Http Httpresponseforbidden
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseForbidden(...,content_type=$TYPE,...)
  - django.http.HttpResponseForbidden(...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.173
  name: Httpresponseforbidden
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseForbidden(...,content_type=$TYPE,...)
  - django.http.HttpResponseForbidden(...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.174
  name: Http Httpresponsenotallowed
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseNotAllowed(...,content_type=$TYPE,...)
  - django.http.HttpResponseNotAllowed(...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.175
  name: Httpresponsenotallowed
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseNotAllowed(...,content_type=$TYPE,...)
  - django.http.HttpResponseNotAllowed(...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.176
  name: Http Httpresponsegone
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseGone(...)
  - django.http.HttpResponseGone(...,content_type=$TYPE,...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.177
  name: Httpresponsegone
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseGone(...)
  - django.http.HttpResponseGone(...,content_type=$TYPE,...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.178
  name: Http Httpresponseservererror
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseServerError(...)
  - django.http.HttpResponseServerError(...,content_type=$TYPE,...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.179
  name: Httpresponseservererror
  ecosystem: django
  category: security
  description: Detected data rendered directly to the end user via 'HttpResponse'
    or a similar object. This bypasses Django's built-in cross-site scripting (XSS)
    defenses and could result in an XSS vulnerability. Us
  patterns:
  - django.http.HttpResponseServerError(...)
  - django.http.HttpResponseServerError(...,content_type=$TYPE,...)
  source_rules:
  - direct-use-of-httpresponse
  tier: T2
- id: EXT.DJANGO.SEC.180
  name: Utils Html Html Safe
  ecosystem: django
  category: security
  description: '`html_safe()` add the `__html__` magic method to the provided class.
    The `__html__` method indicates to the Django template engine that the value is
    ''safe'' for rendering. This means that normal HTML e'
  patterns:
  - django.utils.html.html_safe(...)
  - '@django.utils.html.html_safe'
  source_rules:
  - html-safe
  tier: T2
- id: EXT.DJANGO.SEC.181
  name: Html Safe
  ecosystem: django
  category: security
  description: '`html_safe()` add the `__html__` magic method to the provided class.
    The `__html__` method indicates to the Django template engine that the value is
    ''safe'' for rendering. This means that normal HTML e'
  patterns:
  - django.utils.html.html_safe(...)
  source_rules:
  - html-safe
  tier: T2
- id: EXT.DJANGO.SEC.182
  name: '@Django Utils Html Html Safe'
  ecosystem: django
  category: security
  description: '`html_safe()` add the `__html__` magic method to the provided class.
    The `__html__` method indicates to the Django template engine that the value is
    ''safe'' for rendering. This means that normal HTML e'
  patterns:
  - '@django.utils.html.html_safe'
  source_rules:
  - html-safe
  tier: T2
- id: EXT.DJANGO.SEC.183
  name: Utils Safestring Safestring
  ecosystem: django
  category: security
  description: Found a class extending 'SafeString', 'SafeText' or 'SafeData'. These
    classes are for bypassing the escaping engine built in to Django and should not
    be used directly. Improper use of this class expos
  patterns:
  - 'class $CLASS(django.utils.safestring.SafeString):'
  source_rules:
  - class-extends-safestring
  tier: T2
- id: EXT.DJANGO.SEC.184
  name: Utils Safestring Safetext
  ecosystem: django
  category: security
  description: Found a class extending 'SafeString', 'SafeText' or 'SafeData'. These
    classes are for bypassing the escaping engine built in to Django and should not
    be used directly. Improper use of this class expos
  patterns:
  - 'class $CLASS(django.utils.safestring.SafeText):'
  source_rules:
  - class-extends-safestring
  tier: T2
- id: EXT.DJANGO.SEC.185
  name: Utils Safestring Safedata
  ecosystem: django
  category: security
  description: Found a class extending 'SafeString', 'SafeText' or 'SafeData'. These
    classes are for bypassing the escaping engine built in to Django and should not
    be used directly. Improper use of this class expos
  patterns:
  - 'class $CLASS(django.utils.safestring.SafeData):'
  source_rules:
  - class-extends-safestring
  tier: T2
- id: EXT.DJANGO.SEC.186
  name: Register Filter
  ecosystem: django
  category: security
  description: Detected Django filters flagged with 'is_safe'. 'is_safe' tells Django
    not to apply escaping on the value returned by this filter (although the input
    is escaped). Used improperly, 'is_safe' could expo
  patterns:
  - '@register.filter(..., is_safe=True, ...)'
  source_rules:
  - filter-with-is-safe
  tier: T2
- id: EXT.DJANGO.SEC.187
  name: '@Register Filter'
  ecosystem: django
  category: security
  description: Detected Django filters flagged with 'is_safe'. 'is_safe' tells Django
    not to apply escaping on the value returned by this filter (although the input
    is escaped). Used improperly, 'is_safe' could expo
  patterns:
  - '@register.filter(..., is_safe=True, ...)'
  source_rules:
  - filter-with-is-safe
  tier: T2
- id: EXT.DJANGO.SEC.188
  name: Filter
  ecosystem: django
  category: security
  description: Detected Django filters flagged with 'is_safe'. 'is_safe' tells Django
    not to apply escaping on the value returned by this filter (although the input
    is escaped). Used improperly, 'is_safe' could expo
  patterns:
  - '@register.filter(..., is_safe=True, ...)'
  source_rules:
  - filter-with-is-safe
  tier: T2
- id: EXT.FLASK.SEC.001
  name: Format
  ecosystem: flask
  category: security
  description: User data flows into the host portion of this manually-constructed
    URL. This could allow an attacker to send data to their own server, potentially
    exposing sensitive data such as cookies or authorizat
  patterns:
  - '"$URLSTR".format(...)'
  - return "...".format(...)
  - '"$HTMLSTR".format(...)'
  - $V = "...".format(...)
  - flask.render_template("...".format(...), ...)
  source_rules:
  - unescaped-template-extension
  - tainted-sql-string
  - tainted-url-host
  - directly-returned-format-string
  - dangerous-template-string
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.002
  name: Redirect
  ecosystem: flask
  category: security
  description: Data from request is passed to redirect(). This is an open redirect
    and could be exploited. Consider using 'url_for()' to generate links to known
    locations. If you must use a URL to unknown pages, con
  patterns:
  - flask.redirect(<... flask.request.$W(...) ...>, ...)
  - flask.redirect(<... flask.request.$W ...>, ...)
  - flask.redirect(<... flask.request.$W.get(...) ...>, ...)
  - flask.redirect(<... flask.request.$W[...] ...>, ...)
  source_rules:
  - open-redirect
  tier: T2
- id: EXT.FLASK.SEC.003
  name: Request
  ecosystem: flask
  category: security
  description: Data from request is passed to redirect(). This is an open redirect
    and could be exploited. Consider using 'url_for()' to generate links to known
    locations. If you must use a URL to unknown pages, con
  patterns:
  - $Z = flask.request["host"];
  - eval(..., <... flask.request.$W ...>, ...)
  - exec(..., <... flask.request.$W(...) ...>, ...)
  - flask.redirect(<... flask.request.$W.get(...) ...>, ...)
  - flask.request.$ANYTHING
  source_rules:
  - host-header-injection-python
  - path-traversal-open
  - ssrf-requests
  - eval-injection
  - tainted-sql-string
  - tainted-url-host
  - open-redirect
  - exec-injection
  - os-system-injection
  - nan-injection
  tier: T2
- id: EXT.FLASK.SEC.004
  name: W Get
  ecosystem: flask
  category: security
  description: Data from request is passed to redirect(). This is an open redirect
    and could be exploited. Consider using 'url_for()' to generate links to known
    locations. If you must use a URL to unknown pages, con
  patterns:
  - open(..., <... flask.request.$W.get(...) ...>, ...)
  - requests.$FUNC(..., <... flask.request.$W.get(...) ...>, ...)
  - $V = flask.request.$W.get(...)
  - flask.redirect(<... flask.request.$W.get(...) ...>, ...)
  - os.system(..., <... flask.request.$W.get(...) ...>, ...)
  source_rules:
  - path-traversal-open
  - ssrf-requests
  - eval-injection
  - open-redirect
  - exec-injection
  - os-system-injection
  tier: T2
- id: EXT.FLASK.SEC.005
  name: Redirect
  ecosystem: flask
  category: security
  description: Data from request is passed to redirect(). This is an open redirect
    and could be exploited. Consider using 'url_for()' to generate links to known
    locations. If you must use a URL to unknown pages, con
  patterns:
  - flask.redirect(<... flask.request.$W(...) ...>, ...)
  - flask.redirect(<... flask.request.$W ...>, ...)
  - flask.redirect(<... flask.request.$W.get(...) ...>, ...)
  - flask.redirect(<... flask.request.$W[...] ...>, ...)
  source_rules:
  - open-redirect
  tier: T2
- id: EXT.FLASK.SEC.006
  name: Get
  ecosystem: flask
  category: security
  description: Data from request is passed to redirect(). This is an open redirect
    and could be exploited. Consider using 'url_for()' to generate links to known
    locations. If you must use a URL to unknown pages, con
  patterns:
  - request.$VALUE.get(...)
  - open(..., <... flask.request.$W.get(...) ...>, ...)
  - requests.$FUNC(..., <... flask.request.$W.get(...) ...>, ...)
  - flask.request.headers.get(...)
  - $V = flask.request.$W.get(...)
  source_rules:
  - subprocess-injection
  - path-traversal-open
  - template-unquoted-attribute-var
  - ssrf-requests
  - eval-injection
  - csv-writer-injection
  - open-redirect
  - exec-injection
  - directly-returned-format-string
  - os-system-injection
  tier: T2
- id: EXT.FLASK.SEC.007
  name: Yaml Load
  ecosystem: flask
  category: security
  description: Detected the use of an insecure deserialization library in a Flask
    route. These libraries are prone to code execution vulnerabilities. Ensure user
    data does not enter this function. To fix this, try t
  patterns:
  - yaml.load(...)
  source_rules:
  - insecure-deserialization
  tier: T2
- id: EXT.FLASK.SEC.008
  name: Load
  ecosystem: flask
  category: security
  description: Detected the use of an insecure deserialization library in a Flask
    route. These libraries are prone to code execution vulnerabilities. Ensure user
    data does not enter this function. To fix this, try t
  patterns:
  - yaml.load(...)
  source_rules:
  - insecure-deserialization
  tier: T2
- id: EXT.FLASK.SEC.009
  name: Send File
  ecosystem: flask
  category: security
  description: 'Detected a user-controlled `filename` that could flow to `flask.send_file()`
    function. This could lead to an attacker reading arbitrary file from the system,
    leaking private information. Make sure to '
  patterns:
  - flask.send_file(filename, ...)
  source_rules:
  - avoid_send_file_without_path_sanitization
  tier: T2
- id: EXT.FLASK.SEC.010
  name: Send File
  ecosystem: flask
  category: security
  description: 'Detected a user-controlled `filename` that could flow to `flask.send_file()`
    function. This could lead to an attacker reading arbitrary file from the system,
    leaking private information. Make sure to '
  patterns:
  - flask.send_file(filename, ...)
  source_rules:
  - avoid_send_file_without_path_sanitization
  tier: T2
- id: EXT.FLASK.SEC.011
  name: Request Args Get
  ecosystem: flask
  category: security
  description: 'Flask response reflects unsanitized user input. This could lead to
    a cross-site scripting vulnerability (https://owasp.org/www-community/attacks/xss/)
    in which an attacker causes arbitrary code to be '
  patterns:
  - $X = flask.request.args.get(...)
  - flask.request.args.get(...)
  source_rules:
  - subprocess-injection
  - response-contains-unsanitized-input
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.012
  name: Render Template
  ecosystem: flask
  category: security
  description: Flask does not automatically escape Jinja templates unless they have
    .html, .htm, .xml, or .xhtml extensions. This could lead to XSS attacks. Use .html,
    .htm, .xml, or .xhtml for your template extensi
  patterns:
  - flask.render_template($TPL, ...)
  - flask.render_template($X + "...", ...)
  - flask.render_template("..." % $Y, ...)
  - flask.render_template("...".format(...), ...)
  - flask.render_template("...", ...)
  source_rules:
  - unescaped-template-extension
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.013
  name: Render Template
  ecosystem: flask
  category: security
  description: Flask does not automatically escape Jinja templates unless they have
    .html, .htm, .xml, or .xhtml extensions. This could lead to XSS attacks. Use .html,
    .htm, .xml, or .xhtml for your template extensi
  patterns:
  - flask.render_template($TPL, ...)
  - flask.render_template($X + "...", ...)
  - flask.render_template("..." % $Y, ...)
  - flask.render_template("...".format(...), ...)
  - flask.render_template("...", ...)
  source_rules:
  - unescaped-template-extension
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.014
  name: Hashids Hashids
  ecosystem: flask
  category: security
  description: 'The Flask secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Flask secret key '
  patterns:
  - hashids.Hashids(..., salt=$APP.config['SECRET_KEY'], ...)
  - hashids.Hashids($APP.config['SECRET_KEY'], ...)
  - hashids.Hashids(flask.current_app.config['SECRET_KEY'], ...)
  - hashids.Hashids(..., salt=flask.current_app.config['SECRET_KEY'], ...)
  source_rules:
  - hashids-with-flask-secret
  tier: T2
- id: EXT.FLASK.SEC.015
  name: Current App Config
  ecosystem: flask
  category: security
  description: 'The Flask secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Flask secret key '
  patterns:
  - hashids.Hashids(flask.current_app.config['SECRET_KEY'], ...)
  - hashids.Hashids(..., salt=flask.current_app.config['SECRET_KEY'], ...)
  source_rules:
  - hashids-with-flask-secret
  tier: T2
- id: EXT.FLASK.SEC.016
  name: Hashids
  ecosystem: flask
  category: security
  description: 'The Flask secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Flask secret key '
  patterns:
  - hashids.Hashids(..., salt=$APP.config['SECRET_KEY'], ...)
  - hashids.Hashids($APP.config['SECRET_KEY'], ...)
  - hashids.Hashids(flask.current_app.config['SECRET_KEY'], ...)
  - hashids.Hashids(..., salt=flask.current_app.config['SECRET_KEY'], ...)
  source_rules:
  - hashids-with-flask-secret
  tier: T2
- id: EXT.FLASK.SEC.017
  name: App Config
  ecosystem: flask
  category: security
  description: 'The Flask secret key is used as salt in HashIDs. The HashID mechanism
    is not secure. By observing sufficient HashIDs, the salt used to construct them
    can be recovered. This means the Flask secret key '
  patterns:
  - $APP.config["WTF_CSRF_ENABLED"] = $FALSE
  - hashids.Hashids(..., salt=$APP.config['SECRET_KEY'], ...)
  - hashids.Hashids($APP.config['SECRET_KEY'], ...)
  - $APP.config.$UPDATE(
  source_rules:
  - flask-wtf-csrf-disabled
  - hashids-with-flask-secret
  tier: T2
- id: EXT.FLASK.GEN.018
  name: App Route
  ecosystem: flask
  category: caching
  description: Looks like `$R` is a flask function handler that registered to two
    different routes. This will cause a runtime error
  patterns:
  - '@app.route("...")'
  - '@app.route("...", methods=[..., "POST", ...])'
  - '@app.route("...", ...)'
  - '@app.route("...", methods=[..., "PUT", ...])'
  - '@app.route("...", methods=[..., "PATCH", ...])'
  source_rules:
  - flask-duplicate-handler-name
  - flask-cache-query-string
  tier: T2
- id: EXT.FLASK.GEN.019
  name: '@App Route'
  ecosystem: flask
  category: caching
  description: Looks like `$R` is a flask function handler that registered to two
    different routes. This will cause a runtime error
  patterns:
  - '@app.route("...")'
  - '@app.route("...", methods=[..., "POST", ...])'
  - '@app.route("...", ...)'
  - '@app.route("...", methods=[..., "PUT", ...])'
  - '@app.route("...", methods=[..., "PATCH", ...])'
  source_rules:
  - flask-duplicate-handler-name
  - flask-cache-query-string
  tier: T2
- id: EXT.FLASK.GEN.020
  name: Route
  ecosystem: flask
  category: caching
  description: Looks like `$R` is a flask function handler that registered to two
    different routes. This will cause a runtime error
  patterns:
  - '@app.route("...")'
  - '@app.route("...", methods=[..., "POST", ...])'
  - '@app.route("...", ...)'
  - '@app.route("...", methods=[..., "PUT", ...])'
  - '@app.route("...", methods=[..., "PATCH", ...])'
  source_rules:
  - flask-duplicate-handler-name
  - flask-cache-query-string
  tier: T2
- id: EXT.FLASK.SEC.021
  name: Request Json
  ecosystem: flask
  category: security
  description: Accessing request object inside a route handle for HTTP GET command
    will throw due to missing request body.
  patterns:
  - $Y = flask.request.json
  - flask.request.json
  source_rules:
  - avoid-accessing-request-in-wrong-handler
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.022
  name: Request Form
  ecosystem: flask
  category: security
  description: Accessing request object inside a route handle for HTTP GET command
    will throw due to missing request body.
  patterns:
  - $Y = flask.request.form
  - flask.request.form[...]
  source_rules:
  - avoid-accessing-request-in-wrong-handler
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.023
  name: Request Data
  ecosystem: flask
  category: security
  description: Accessing request object inside a route handle for HTTP GET command
    will throw due to missing request body.
  patterns:
  - $Y = flask.request.data
  - flask.request.data
  source_rules:
  - avoid-accessing-request-in-wrong-handler
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.BEST.024
  name: Json Dumps
  ecosystem: flask
  category: best-practice
  description: flask.jsonify() is a Flask helper method which handles the correct  settings
    for returning JSON from Flask routes
  patterns:
  - json.dumps($...VAR)
  source_rules:
  - use-jsonify
  tier: T2
- id: EXT.FLASK.BEST.025
  name: Dumps
  ecosystem: flask
  category: best-practice
  description: flask.jsonify() is a Flask helper method which handles the correct  settings
    for returning JSON from Flask routes
  patterns:
  - json.dumps($...VAR)
  source_rules:
  - use-jsonify
  tier: T2
- id: EXT.FLASK.MAINT.026
  name: App Open Session
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.open_session(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.027
  name: Open Session
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.open_session(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.028
  name: App Save Session
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.save_session(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.029
  name: Save Session
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.save_session(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.030
  name: App Make Null Session
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.make_null_session(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.031
  name: Make Null Session
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.make_null_session(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.032
  name: App Init Jinja Globals
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.init_jinja_globals(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.033
  name: Init Jinja Globals
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.init_jinja_globals(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.034
  name: App Request Globals Class
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.request_globals_class(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.035
  name: Request Globals Class
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.request_globals_class(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.036
  name: App Static Path
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.static_path(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.037
  name: Static Path
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.static_path(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.038
  name: App Config From Json
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.config.from_json(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.039
  name: From Json
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - app.config.from_json(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.040
  name: Json Available
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - flask.json_available
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.041
  name: Request Module
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - flask.request.module
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.042
  name: Testing Make Test Environ Builder
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - flask.testing.make_test_environ_builder(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.MAINT.043
  name: Make Test Environ Builder
  ecosystem: flask
  category: maintainability
  description: deprecated Flask API
  patterns:
  - flask.testing.make_test_environ_builder(...)
  source_rules:
  - flask-deprecated-apis
  tier: T2
- id: EXT.FLASK.SEC.044
  name: App Run
  ecosystem: flask
  category: security
  description: Detected Flask app with debug=True. Do not deploy to production with
    this flag enabled as it will leak sensitive information. Instead, consider using
    Flask configuration variables or setting 'debug' u
  patterns:
  - $APP.run(..., debug=True, ...)
  - app.run(..., host="0.0.0.0", ...)
  - app.run(...)
  - app.run(..., "0.0.0.0", ...)
  source_rules:
  - debug-enabled
  - avoid_using_app_run_directly
  - avoid_app_run_with_bad_host
  tier: T2
- id: EXT.FLASK.SEC.045
  name: Run
  ecosystem: flask
  category: security
  description: Detected Flask app with debug=True. Do not deploy to production with
    this flag enabled as it will leak sensitive information. Instead, consider using
    Flask configuration variables or setting 'debug' u
  patterns:
  - $APP.run(..., debug=True, ...)
  - app.run(..., host="0.0.0.0", ...)
  - app.run(...)
  - app.run(..., "0.0.0.0", ...)
  source_rules:
  - debug-enabled
  - avoid_using_app_run_directly
  - avoid_app_run_with_bad_host
  tier: T2
- id: EXT.FLASK.SEC.046
  name: Request Host
  ecosystem: flask
  category: security
  description: The `flask.request.host` is used to construct an HTTP request.  This
    can lead to host header injection issues. Vulnerabilities  that generally occur
    due to this issue are authentication bypasses,  pas
  patterns:
  - $X = <... "=~/.*http[s]*:///" + flask.request.host ...>;
  - $Z = flask.request.host;
  source_rules:
  - host-header-injection-python
  tier: T2
- id: EXT.FLASK.SEC.047
  name: '@Cross Origin'
  ecosystem: flask
  category: security
  description: Setting 'support_credentials=True' together with 'origin="*"' is a
    CORS misconfiguration that can allow third party origins to read sensitive data.
    Using this configuration, flask_cors will dynamicall
  patterns:
  - '@cross_origin(..., origins="*", supports_credentials=True, ...)'
  source_rules:
  - flask-cors-misconfiguration
  tier: T2
- id: EXT.FLASK.SEC.048
  name: Url For
  ecosystem: flask
  category: security
  description: Function `flask.url_for` with `_external=True` argument will generate
    URLs using the `Host` header of the HTTP request, which may lead to security risks
    such as Host header injection
  patterns:
  - flask.url_for(..., _external=$VAR, ...)
  source_rules:
  - flask-url-for-external-true
  tier: T2
- id: EXT.FLASK.SEC.049
  name: Url For
  ecosystem: flask
  category: security
  description: Function `flask.url_for` with `_external=True` argument will generate
    URLs using the `Host` header of the HTTP request, which may lead to security risks
    such as Host header injection
  patterns:
  - flask.url_for(..., _external=$VAR, ...)
  source_rules:
  - flask-url-for-external-true
  tier: T2
- id: EXT.FLASK.SEC.050
  name: Resp Set Cookie
  ecosystem: flask
  category: security
  description: Found a Flask cookie with insecurely configured properties.  By default
    the secure, httponly and samesite ar configured insecurely. cookies should be
    handled securely by setting `secure=True`, `httpon
  patterns:
  - $RESP.set_cookie(...)
  source_rules:
  - secure-set-cookie
  tier: T2
- id: EXT.FLASK.SEC.051
  name: Set Cookie
  ecosystem: flask
  category: security
  description: Found a Flask cookie with insecurely configured properties.  By default
    the secure, httponly and samesite ar configured insecurely. cookies should be
    handled securely by setting `secure=True`, `httpon
  patterns:
  - $RESP.set_cookie(...)
  source_rules:
  - secure-set-cookie
  tier: T2
- id: EXT.FLASK.SEC.052
  name: App Config Wtf Csrf Enabled
  ecosystem: flask
  category: security
  description: Setting 'WTF_CSRF_ENABLED' to 'False' explicitly disables CSRF protection.
  patterns:
  - $APP.config.WTF_CSRF_ENABLED = $FALSE
  source_rules:
  - flask-wtf-csrf-disabled
  tier: T2
- id: EXT.FLASK.SEC.053
  name: M Config
  ecosystem: flask
  category: security
  description: Hardcoded variable `TESTING` detected. Use environment variables or
    config files instead
  patterns:
  - $M.config['DEBUG'] = True
  - $M.config['DEBUG'] = False
  - $M.config['ENV'] = "=~/^development|production$/"
  - $M.config['TESTING'] = True
  - $M.config['SECRET_KEY'] = "=~/.*/"
  source_rules:
  - avoid_hardcoded_config_ENV
  - avoid_hardcoded_config_SECRET_KEY
  - avoid_hardcoded_config_TESTING
  - avoid_hardcoded_config_DEBUG
  tier: T2
- id: EXT.FLASK.SEC.054
  name: M Update
  ecosystem: flask
  category: security
  description: Hardcoded variable `TESTING` detected. Use environment variables or
    config files instead
  patterns:
  - $M.update(DEBUG=False)
  - $M.update(ENV="=~/^development|production$/")
  - $M.update(DEBUG=True)
  - $M.update(TESTING=True, ...)
  - $M.update(SECRET_KEY="=~/.*/")
  source_rules:
  - avoid_hardcoded_config_ENV
  - avoid_hardcoded_config_SECRET_KEY
  - avoid_hardcoded_config_TESTING
  - avoid_hardcoded_config_DEBUG
  tier: T2
- id: EXT.FLASK.SEC.055
  name: Update
  ecosystem: flask
  category: security
  description: Hardcoded variable `TESTING` detected. Use environment variables or
    config files instead
  patterns:
  - $M.update(DEBUG=False)
  - $M.update(ENV="=~/^development|production$/")
  - $M.update(DEBUG=True)
  - $M.update(TESTING=True, ...)
  - $M.update(SECRET_KEY="=~/.*/")
  source_rules:
  - avoid_hardcoded_config_ENV
  - avoid_hardcoded_config_SECRET_KEY
  - avoid_hardcoded_config_TESTING
  - avoid_hardcoded_config_DEBUG
  tier: T2
- id: EXT.FLASK.SEC.056
  name: Func Get
  ecosystem: flask
  category: security
  description: Detected Flask route directly returning a formatted string. This is
    subject to cross-site scripting if user input can reach the string. Consider using
    the template engine instead and rendering pages w
  patterns:
  - request.$FUNC.get(...)
  source_rules:
  - directly-returned-format-string
  tier: T2
- id: EXT.FLASK.SEC.057
  name: Render Template String
  ecosystem: flask
  category: security
  description: Found a template created with string formatting. This is susceptible
    to server-side template injection and cross-site scripting attacks.
  patterns:
  - flask.render_template_string(...)
  source_rules:
  - render-template-string
  tier: T2
- id: EXT.FLASK.SEC.058
  name: Render Template String
  ecosystem: flask
  category: security
  description: Found a template created with string formatting. This is susceptible
    to server-side template injection and cross-site scripting attacks.
  patterns:
  - flask.render_template_string(...)
  source_rules:
  - render-template-string
  tier: T2
- id: EXT.FLASK.SEC.059
  name: Jinja2 Escape
  ecosystem: flask
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - jinja2.escape(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.060
  name: Escape
  ecosystem: flask
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - jinja2.escape(...)
  - flask.escape(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.061
  name: Escape
  ecosystem: flask
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - flask.escape(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.062
  name: Html Format
  ecosystem: flask
  category: security
  description: Detected user input flowing into a manually constructed HTML string.
    You may be accidentally bypassing secure methods of rendering HTML by manually
    constructing HTML and this could create a cross-site
  patterns:
  - $HTML.format(...)
  source_rules:
  - raw-html-format
  tier: T2
- id: EXT.FLASK.SEC.063
  name: Something Get
  ecosystem: flask
  category: security
  description: Found user input going directly into typecast for bool(), float(),
    or complex(). This allows an attacker to inject Python's not-a-number (NaN) into
    the typecast. This results in undefind behavior, par
  patterns:
  - flask.request.$SOMETHING.get(...)
  source_rules:
  - nan-injection
  tier: T2
- id: EXT.FLASK.SEC.064
  name: Request Form Get
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.form.get(...)
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.065
  name: Request Args
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.args[...]
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.066
  name: Request Values Get
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.values.get(...)
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.067
  name: Request Values
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.values[...]
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.068
  name: Request Cookies Get
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.cookies.get(...)
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.069
  name: Request Cookies
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.cookies[...]
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.070
  name: Request Stream
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.stream
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.071
  name: Request Headers Get
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.headers.get(...)
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.072
  name: Request Headers
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.headers[...]
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.073
  name: Request Full Path
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.full_path
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.074
  name: Request Url
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.url
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.075
  name: Request Get Json
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.get_json()
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.076
  name: Get Json
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.get_json()
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.077
  name: Request View Args Get
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.view_args.get(...)
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.078
  name: Request View Args
  ecosystem: flask
  category: security
  description: Detected user input into a generated CSV file using the built-in `csv`
    module. If user data is used to generate the data in this file, it is possible
    that an attacker could inject a formula when the C
  patterns:
  - flask.request.view_args[...]
  source_rules:
  - subprocess-injection
  - csv-writer-injection
  tier: T2
- id: EXT.FLASK.SEC.079
  name: Os System
  ecosystem: flask
  category: security
  description: User data detected in os.system. This could be vulnerable to a command
    injection and should be avoided. If this must be done, use the 'subprocess' module
    instead and pass the arguments as a list.
  patterns:
  - os.system(...)
  - os.system(..., <... flask.request.$W.get(...) ...>, ...)
  - os.system(..., <... flask.request.$W[...] ...>, ...)
  - os.system(..., <... flask.request.$W ...>, ...)
  - os.system(..., <... flask.request.$W(...) ...>, ...)
  source_rules:
  - os-system-injection
  tier: T2
- id: EXT.FLASK.SEC.080
  name: System
  ecosystem: flask
  category: security
  description: User data detected in os.system. This could be vulnerable to a command
    injection and should be avoided. If this must be done, use the 'subprocess' module
    instead and pass the arguments as a list.
  patterns:
  - os.system(...)
  - os.system(..., <... flask.request.$W.get(...) ...>, ...)
  - os.system(..., <... flask.request.$W[...] ...>, ...)
  - os.system(..., <... flask.request.$W ...>, ...)
  - os.system(..., <... flask.request.$W(...) ...>, ...)
  source_rules:
  - os-system-injection
  tier: T2
- id: EXT.FLASK.SEC.081
  name: Make Response
  ecosystem: flask
  category: security
  description: Be careful with `flask.make_response()`. If this response is rendered
    onto a webpage, this could create a cross-site scripting (XSS) vulnerability.
    `flask.make_response()` will not autoescape HTML. If
  patterns:
  - flask.make_response(...)
  source_rules:
  - make-response-with-unknown-content
  tier: T2
- id: EXT.FLASK.SEC.082
  name: Make Response
  ecosystem: flask
  category: security
  description: Be careful with `flask.make_response()`. If this response is rendered
    onto a webpage, this could create a cross-site scripting (XSS) vulnerability.
    `flask.make_response()` will not autoescape HTML. If
  patterns:
  - flask.make_response(...)
  source_rules:
  - make-response-with-unknown-content
  tier: T2
- id: EXT.FLASK.SEC.083
  name: Jinja2 Environment
  ecosystem: flask
  category: security
  description: Detected direct use of jinja2. If not done properly, this may bypass
    HTML escaping which opens up the application to cross-site scripting (XSS) vulnerabilities.
    Prefer using the Flask method 'render_t
  patterns:
  - jinja2.Environment(...)
  source_rules:
  - direct-use-of-jinja2
  tier: T2
- id: EXT.FLASK.SEC.084
  name: Environment
  ecosystem: flask
  category: security
  description: Detected direct use of jinja2. If not done properly, this may bypass
    HTML escaping which opens up the application to cross-site scripting (XSS) vulnerabilities.
    Prefer using the Flask method 'render_t
  patterns:
  - jinja2.Environment(...)
  source_rules:
  - direct-use-of-jinja2
  tier: T2
- id: EXT.FLASK.SEC.085
  name: Jinja2 Template Render
  ecosystem: flask
  category: security
  description: Detected direct use of jinja2. If not done properly, this may bypass
    HTML escaping which opens up the application to cross-site scripting (XSS) vulnerabilities.
    Prefer using the Flask method 'render_t
  patterns:
  - jinja2.Template.render(...)
  source_rules:
  - direct-use-of-jinja2
  tier: T2
- id: EXT.FLASK.SEC.086
  name: Render
  ecosystem: flask
  category: security
  description: Detected direct use of jinja2. If not done properly, this may bypass
    HTML escaping which opens up the application to cross-site scripting (XSS) vulnerabilities.
    Prefer using the Flask method 'render_t
  patterns:
  - jinja2.Template.render(...)
  - $TEMPLATE.render(...)
  source_rules:
  - direct-use-of-jinja2
  tier: T2
- id: EXT.FLASK.SEC.087
  name: Template Render
  ecosystem: flask
  category: security
  description: Detected direct use of jinja2. If not done properly, this may bypass
    HTML escaping which opens up the application to cross-site scripting (XSS) vulnerabilities.
    Prefer using the Flask method 'render_t
  patterns:
  - $TEMPLATE.render(...)
  source_rules:
  - direct-use-of-jinja2
  tier: T2
- id: EXT.FLASK.SEC.088
  name: Value Get
  ecosystem: flask
  category: security
  description: 'Detected a unquoted template variable as an attribute. If unquoted,
    a malicious actor could inject custom JavaScript handlers. To fix this, add quotes
    around the template expression, like this: "{{ $.'
  patterns:
  - request.$VALUE.get(...)
  source_rules:
  - template-unquoted-attribute-var
  tier: T2
- id: EXT.FLASK.SEC.089
  name: Markup Unescape
  ecosystem: flask
  category: security
  description: Detected explicitly unescaped content using 'Markup()'. This permits
    the unescaped data to include unescaped HTML which could result in cross-site
    scripting. Ensure this data is not externally control
  patterns:
  - flask.Markup.unescape(...)
  source_rules:
  - explicit-unescape-with-markup
  tier: T2
- id: EXT.FLASK.SEC.090
  name: Unescape
  ecosystem: flask
  category: security
  description: Detected explicitly unescaped content using 'Markup()'. This permits
    the unescaped data to include unescaped HTML which could result in cross-site
    scripting. Ensure this data is not externally control
  patterns:
  - $MARKUPOBJ.unescape()
  - flask.Markup.unescape(...)
  source_rules:
  - explicit-unescape-with-markup
  tier: T2
- id: EXT.FLASK.SEC.091
  name: Markupobj Unescape
  ecosystem: flask
  category: security
  description: Detected explicitly unescaped content using 'Markup()'. This permits
    the unescaped data to include unescaped HTML which could result in cross-site
    scripting. Ensure this data is not externally control
  patterns:
  - $MARKUPOBJ.unescape()
  source_rules:
  - explicit-unescape-with-markup
  tier: T2
- id: EXT.FLASK.SEC.092
  name: Markup
  ecosystem: flask
  category: security
  description: Detected explicitly unescaped content using 'Markup()'. This permits
    the unescaped data to include unescaped HTML which could result in cross-site
    scripting. Ensure this data is not externally control
  patterns:
  - flask.Markup($Q)
  source_rules:
  - explicit-unescape-with-markup
  tier: T2
- id: EXT.FLASK.SEC.093
  name: Markup
  ecosystem: flask
  category: security
  description: Detected explicitly unescaped content using 'Markup()'. This permits
    the unescaped data to include unescaped HTML which could result in cross-site
    scripting. Ensure this data is not externally control
  patterns:
  - flask.Markup($Q)
  - markupsafe.Markup($Q)
  source_rules:
  - explicit-unescape-with-markup
  tier: T2
- id: EXT.FLASK.SEC.094
  name: Markupsafe Markup
  ecosystem: flask
  category: security
  description: Detected explicitly unescaped content using 'Markup()'. This permits
    the unescaped data to include unescaped HTML which could result in cross-site
    scripting. Ensure this data is not externally control
  patterns:
  - markupsafe.Markup($Q)
  source_rules:
  - explicit-unescape-with-markup
  tier: T2
- id: EXT.FASTAPI.SEC.001
  name: App Add Middleware
  ecosystem: fastapi
  category: security
  description: CORS policy allows any origin (using wildcard '*'). This is insecure
    and should be avoided.
  patterns:
  - $APP.add_middleware(
  source_rules:
  - wildcard-cors
  tier: T2
- id: EXT.FASTAPI.SEC.002
  name: Add Middleware
  ecosystem: fastapi
  category: security
  description: CORS policy allows any origin (using wildcard '*'). This is insecure
    and should be avoided.
  patterns:
  - $APP.add_middleware(
  source_rules:
  - wildcard-cors
  tier: T2
