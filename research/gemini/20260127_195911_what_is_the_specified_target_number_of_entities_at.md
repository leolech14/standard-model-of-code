# Research: What is the specified target number of entities at the project root (Level 0)? Is there a formal rul...

> **Date:** 2026-01-27 19:59:11
> **Source:** gemini
> **Model:** gemini-2.5-flash
> **Checksum:** `sha256:abbcab4f64bce13ee3f33e1b18df9387aefaa258cd11b219ade4d42d07b4eb4c`
> **Raw JSON:** `raw/20260127_195911_what_is_the_specified_target_number_of_entities_at.json`

---

## Query

What is the specified target number of entities at the project root (Level 0)? Is there a formal rule for what is allowed to stay in root versus what must be in Brain/Body?

---

## Response

Based on the provided `PROJECTOME.md` and `PROJECTOME_TOTALITY_REPORT.md` documents:

1.  **Specified target number of entities at the project root (Level 0):**
    The documents **do not specify a concrete target number** of entities for the project root (Level 0).
    However, the `PROJECTOME_TOTALITY_REPORT.md` explicitly identifies "root-level `*.md` clutter" as an example of "Asymmetric Scatter" and "Dark Matter (Shadow Debt)". This implies that such files are undesirable and contribute to "Active Drift" (Dynamic Entropy). While no specific number is given, the overarching philosophy suggests that the ideal or target number of unaligned files at the root level is effectively **zero**, as they represent "shadow debt" or "dark matter."

2.  **Formal rule for what is allowed to stay in root versus what must be in Brain/Body:**
    Yes, there is a clear, overarching formal rule, albeit one that implies root-level files are generally to be avoided unless they explicitly conform. The core principles are:
    *   **Partitioning:** `PROJECTOME = CODOME ⊔ CONTEXTOME`. This means that **every single file in the project must belong to either the Codome (executable code, "The Body") or the Contextome (non-executable content, "The Brain")**. There is no third category for files that exist outside this "Brain/Body split."
    *   **Concordance Alignment:** "Every file must have a **Waybill** linking it to a **Concordance**." A Concordance is a "purpose-aligned region across Codome + Contextome." Files without such alignment are considered "Dark Matter."
    *   **Implicit Exclusion of Root-Level Files:** The mention of "root-level `*.md` clutter that exist outside the Brain/Body split" as an "Asymmetric Scatter" indicates that files merely existing at the root level without being explicitly categorized into Codome or Contextome (and ideally, a Concordance) are seen as a deviation from the ideal state.

    Therefore, the formal rule is that **all project files, including any that might be at the root level, must be categorized as either Codome or Contextome files and ideally linked to a specific Concordance**. Files that reside at the root but do not clearly fit this classification are considered problematic "clutter" or "dark matter." The framework heavily implies that files should be organized into purpose-aligned directories within the Codome or Contextome rather than remaining at the project root.

---

## Citations

_No citations provided_
