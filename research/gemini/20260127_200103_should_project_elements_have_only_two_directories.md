# Research: Should Project Elements have only TWO directories at the root level (standard-model-of-code and cont...

> **Date:** 2026-01-27 20:01:03
> **Source:** gemini
> **Model:** gemini-2.5-flash
> **Checksum:** `sha256:56d4e3b25051ef1ba7976c168edb194cbc99c0054c7111ad56de27d4966bf5e3`
> **Raw JSON:** `raw/20260127_200103_should_project_elements_have_only_two_directories.json`

---

## Query

Should Project Elements have only TWO directories at the root level (standard-model-of-code and context-management)? Evaluate if directories like 'archive', 'assets', and 'experiments' are allowed to remain at the root or if they also violate the Brain/Body split.

---

## Response

Based on the `PROJECTOME_TOTALITY_REPORT.md` and its fundamental "Algebra of Totality" ($P = C \sqcup X$), Project Elements should indeed strive to have **only two primary directories at the root level**, representing the Codome (C) and Contextome (X).

The proposed structure of `standard-model-of-code` (for the Codome) and `context-management` (for the Contextome) directly aligns with the document's core philosophy of the "Brain/Body split."

Let's evaluate `archive`, `assets`, and `experiments` against this strict definition:

1.  **`experiments/`**:
    *   The report explicitly states: "Asymmetric Scatter: Orphaned `experiments/` and root-level `*.md` clutter that exist outside the Brain/Body split."
    *   This is identified as a source of "Active Drift" and contributes to "Dark Matter" (Shadow Debt).
    *   **Conclusion**: `experiments/` is *not allowed* to remain at the root. Its contents must be either integrated into the `standard-model-of-code` (if executable) or `context-management` (if non-executable like notes or data), or completely removed if they lack "Concordance" and "Topology of Purpose."

2.  **`archive/`**:
    *   While not explicitly mentioned, `archive/` at the root level would similarly violate the "disjoint union" principle ($P = C \sqcup X$). If it sits alongside `standard-model-of-code` and `context-management`, it implies a third primary universe, which contradicts the stated algebra.
    *   The contents of `archive/` are either executable code or non-executable information. Therefore, any *valuable* archived items should reside *within* the appropriate Codome or Contextome structure (e.g., `standard-model-of-code/archive/` or `context-management/docs/archive/`).
    *   **Conclusion**: `archive/` is *not allowed* to remain at the root. Its presence contributes to "Asymmetric Scatter" and "Dark Matter."

3.  **`assets/`**:
    *   Similar to `archive/`, `assets/` at the root level would also violate the fundamental `P = C \sqcup X` equation.
    *   Assets (e.g., images, static files) are typically supporting files for either the executable components (e.g., UI assets for `viz/`) or documentation (e.g., diagrams for `.md` files).
    *   Therefore, `assets/` should be placed *within* the relevant `standard-model-of-code` or `context-management` structure. For example, UI assets might be `standard-model-of-code/viz/assets/`, and documentation images might be `context-management/docs/assets/`.
    *   **Conclusion**: `assets/` is *not allowed* to remain at the root. Its presence contributes to "Asymmetric Scatter."

**Overall Evaluation:**

The `PROJECTOME_TOTALITY_REPORT.md` advocates for a highly structured and auditable "Topology of Purpose" where every file has a "Waybill linking it to a Concordance." The presence of root-level directories like `archive`, `assets`, and `experiments` that are not explicitly part of the Codome or Contextome creates "Asymmetric Scatter" and "Dark Matter," which are considered indicators of "Active Drift" and "Shadow Debt."

Therefore, to achieve the ideal "Projectome" state, **all directories outside of the proposed `standard-model-of-code` and `context-management` must be either eliminated or strictly integrated as sub-directories within one of these two primary universes.** This ensures that the codebase strictly adheres to the "Brain/Body split" and the "Algebra of Totality."

---

## Citations

_No citations provided_
